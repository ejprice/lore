# 26 — Odoo onboarding · BUILD (placeholder — packets minted by 25) · formerly PKT-35+
size: ≤0.25 wu EACH, count set by packet 25's ruled design · wave O · depends: packet 25 ruled; packet 23 (worktree overlay) must land before this series completes

## What this file is
A reserved slot, not a spec. Packet 25 (Odoo onboarding design) mints the actual build
packets here as **26a, 26b, …** — each with its own entry/exit per the sizing law — and
replaces this placeholder's contents with the minted list.

## Expected shape (from packet 25's scope; the ruling decides)
XML reference extractor · MRO/`_inherit`/`_inherits` + field-string capture ·
manifest/csv chunkers · tier layout (static core/enterprise/OCA + live custom) ·
cutover from odoo-code per the operator-struck parity matrix.

## Insertion rule (INDEX protocol)
Mid-sequence insertions anywhere in the plan use letter suffixes (13a/13b for a split
at kickoff; 26a/26b here) — the top-level 01–41 sequence is never renumbered again.
