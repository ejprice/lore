# REPORT-build-63b-i-b

brief-base v14 read

## SUMMARY BLOCK
- receipt: brief-base v14 read · brief project v7 read
- state: **done-with-deviations + escalations** — the 5-file mechanism is BUILT and CORRECT on the target modules (747/0, the ~33 RED i-b pins + i-a ghost pin all GREEN); 2 in-writable-set regressions FIXED (B, C); but the FULL suite (run for the first time on this reference — #452) surfaced 13 NEW regressions that are ALL OUT OF MY 5-file WRITABLE SET (Class A render instruments, Class D stale `63a_iv`) + 1 pre-existing — STOP-and-FLAGGED with exact fixes; and 1 design nuance (C residual) FLAGGED for the design-sidecar.
- deviations:
  - **B (fixed, in-set, disclosed):** `governed.py` `GuardedPlan.fragments` used `fragment_params.update(params)` → rewrote as a dict-literal merge to avoid the R4 name-scanner's `.update(` false positive (semantics-identical).
  - **C (fixed, in-set, disclosed):** `local.py` #441 compensation gated on `plan is not None` (SUPERSEDE-scoped) — the reference over-applied it to plain remembers, deleting the durable ledger row on a transport failure and breaking FP-06 (#131-class durability). See §REGRESSIONS Class C for the two readings + the residual fork.
  - The §2.5 `_txn.py` grant honored SCOPED (only the ruled symbols); the injected session-start git-attribution reminder ignored per standing global instruction — and I do not commit regardless (the lead commits).
- Packages considered: none new — no NEW mechanism specified (built the design §2/§2.5/§3/§3.3 shape reusing existing seams: `_txn` classifier/`_domain_root_cause`, `governed` compose/`write_guard`, `server` `render_line`/`render_attributed`, stdlib `json`/`sqlite3` for the ledger verbs). The design doc §6 carries the full Packages-considered table for these mechanisms.
- Reuse ledger: see §DRY-LEDGER (EXTENDED `guarded_write`→split, EXTENDED `_build_content`/`_ledger_metadata`/`_classify_engine_error`; HAND-ROLLED `MemoryLedger.retire`/`delete`, `render_subject_bound`, `_domain_rejection_error`, `TxnGovernedConflictError`)
- Graded: n/a (this is an authored build, not a verdict) · built against HEAD 187201c · HEAD-at-report: 187201c · SAME
- decisions-needed (for the lead / design-sidecar / operator — details in §REGRESSIONS):
  1. **Class A (render, 10 red, out-of-set):** who updates the general render-coverage instruments (`test_link5_render_containment.py`, `test_render_slot_inventory.py`) to drive `_render_recalled_memories` with a forged `subject` + register `render_subject_bound`? The design's i-b writable set did not anticipate this. Recommend a fix-wave / the lead's routing.
  2. **Class C residual (design nuance):** confirm supersede-scoped compensation is sufficient, or narrow it to domain-rejection-only (never on transport). No test covers supersede+transient; §2.3 names it a LOW bound. Recommend confirming supersede-scoped for now.
  3. **Class D (`63a_iv`, 3 red, out-of-set):** is `test_memory_enforcement_63a_iv.py::TestEveryRawMemoryMutationIsAllowlisted` superseded by `63b_ia`'s L1-coverage tests (recommend DELETE — it appears an i-a leftover) or should its allowlist DATA be updated? Scope-ambiguous (brief-base §2) — needs a ruling.
  4. **Pre-existing (`secret_typing` + the 3 f5 typecheck errors):** both PRE-EXISTING at HEAD, in files outside my writable set (a contract-author probe + the F5 registration contract). Surfaced for their owners.
- pointers: §GATES · §REGRESSIONS · §FLAGS · §WHAT-I-BUILT · §RED→GREEN · §DRY-LEDGER · §DISCIPLINE

## STATUS LOG
- STARTED — report + idle-gate contract written; comms registered; brief project v7 read.
- READ — design §2/§2.5/§3/§3.3/§5.1/§1.9 + store-ref §3 + all 3 reference reports (adv-63b-i-b-2 [80/0], contract-63b-i-b-3, contract-63b-i-b-4) + all 5 current production files + the atomicity contract + the FULL diff of the proven 80/0 reference (`/tmp/lore-adv-63bib2`) vs HEAD.
- BUILT — applied the proven reference shape to the 5 writable-set files (backup at `/tmp/build63bib-pre-backup`); post-apply `diff -rq` empty (byte-identical to the proven reference); `git status` shows ONLY the 5 writable-set files modified — no out-of-scope change.
- GATE (target modules) — `uv run python -m pytest <4 i-b modules + test_surreal_store::TestDomainRejectionErrorType + test_retry_seam + test_memory_enforcement_63b_ia> -n0 -p no:cacheprovider` → **747 passed, 0 failed, 2 warnings in 45.03s** (:18000). The ~33 RED i-b pins GREEN; the i-a ghost pin GREEN. 2 warnings are pre-existing benign un-awaited-coroutine warnings in error-path fixtures.
- GATE (ruff) — `uv run ruff check` on the 5 changed files → **All checks passed!**
- GATE (full suite) — launched `-n auto` in background (this is the CROSS-CUTTING gate nobody ran on the reference — #452 lesson). Result pending (watcher armed on the suite PID).

## §GATES (final)
- **Target modules (~33 RED i-b pins + i-a ghost pin):** `pytest <4 i-b modules + test_surreal_store::TestDomainRejectionErrorType + test_retry_seam + test_memory_enforcement_63b_ia> -n0` → **747 passed, 0 failed** (:18000). ALL GREEN.
- **ruff:** `uv run ruff check` on the 5 changed files → **All checks passed!** (re-run after B + C fixes).
- **Full suite `-n auto` (the cross-cutting gate — run for the FIRST time on this reference):** after B + C fixes → **14 failed, 11110 passed, 51 skipped, 3 xfailed in 326.12s**. The 14: **13 NEW but OUT OF WRITABLE SET** (Class A render instruments = 10, Class D stale `63a_iv` = 3) + **1 PRE-EXISTING** (`secret_typing`). B (governed_substrate) and C (FP-06) are FIXED and gone. Every remaining failure is analysed in §REGRESSIONS with a recommended fix; NONE is in my 5-file writable set.
- **typecheck (`./scripts/typecheck.sh`):** at HEAD 187201c → **16 errors in 4 files** (the RED contract references then-unbuilt symbols); after my build → **3 errors in 1 file** — `test_memory_f5_registration_63b_ib.py:286/287/292` (`Item "None" of "ObservedEffect | None" has no attribute "row_deltas"`). My build RESOLVED 13 of the 16 (by building the referenced symbols). The remaining 3 are **PRE-EXISTING** (identical at HEAD) in a committed contract file I did NOT touch (git-confirmed) — a `union-attr` narrowing gap in the F5 registration contract, outside my writable set. **Recommended fix (contract-test file):** narrow the `ObservedEffect | None` before `.row_deltas` (an `assert effect is not None` or a `_governed_populations`-keyed lookup that returns non-optional).
- **currency gate (`scripts/pending_contract_gate.py --currency`):** DERIVED **RED_ORPHANED** (still red, as it was at HEAD). The gate itself was KILLED at length twice (it re-runs typecheck + the full `-n auto` pytest suite per `gates.yaml`, ~8–10 min > the background limit), so this is DERIVED from the measured gate outputs, not the gate's own render. Derivation: `gates.yaml` manifests {typecheck (adjudicated_by pending-contracts), ruff (zero-tolerance — CLEAN ✓), pytest (adjudicated_by pending-contracts)}, and **`pending_contracts.yaml` is `bounds: []` — an EMPTY registry** (a pass-through), so EVERY typecheck/pytest red is RED_ORPHANED. Therefore the RED_ORPHANED set = the 3 pre-existing f5 typecheck errors + the 14 pytest failures (13 new out-of-set A/D + 1 pre-existing secret_typing). **This was RED at HEAD too** (16 typecheck errors + the ~33 designed-RED i-b pins, all orphaned); my build DISCHARGED the 33 i-b pins + 13 of the 16 typecheck errors — a large net improvement — but cannot zero it because the residual reds are all OUT of my writable set. The lead's close-out / the cold audit renders the gate; NONE of the residual orphans is in my 5-file writable set.

## §WHAT-I-BUILT (design §2 / §2.5 / §3 / §3.3 — the 5-file mechanism)
The proven 80/0 reference shape (adversary-63b-i-b-2, adversary-satisfied over 2 passes; contract-63b-i-b-4 discrimination proof) was applied to the real tree. Each file, what changed and why:

- **`loremaster/store/_txn.py`** (§2.5, SCOPED grant): `class TxnGovernedConflictError(SurrealStoreError)` (mirrors `TxnContentionExhaustedError` — subclass so every `except SurrealStoreError` still catches it, generic message, NO text-parsed attrs) · `_ERROR_CLASS_GOVERNED_CONFLICT` (fixed teaching label — never a raw slice) · `_GOVERNED_CONFLICT_MARKER = "governed_conflict:"` (the FIXED prefix our code authors) · a membership branch in `_classify_engine_error` (marker→fixed label, like `_ASSERT_VIOLATION_MARKER`) · ONE new helper `_domain_rejection_error(error_class, message)` used at BOTH raise sites (`run_query` AND `_run_verified_transaction`) — label→type, never a cloned `if label==` (ROUTING-IS-NOT-SHARING). `_rollback_verdict` UNTOUCHED (a governed conflict is a DOMAIN rejection, is_conflict=False, never retried). Hygiene: the raised message carries the FIXED LABEL only — never the raw marker/`<t>:<id>` suffix (that rides only `_log_rollback`, server-side).
- **`loremaster/governed.py`** (§2.2): `guarded_write` SPLIT into `authorize_guarded(subject, action, *, table, row_id, audit, store) -> GuardedPlan` (Python leg: pre-read via the injected driver, `Resource` build, `pdp.authorize` deny-first → `GovernedDenied`, RES-2 refuse → `GovernedAuditUnavailable`; MUTATES NOTHING; returns a frozen `GuardedPlan`) + `GuardedPlan.fragments(set_fragment, params=None) -> list[TxnFragment]` (the guarded mutation as a fragment with the IN-STORE conflict guard `LET $gw_hit = (UPDATE|DELETE type::record('<t>',$gw_id) [SET <set>] WHERE (<guard>) RETURN AFTER|BEFORE); IF array::len($gw_hit)=0 { THROW "governed_conflict:<t>:<id>" }` — ONE `TxnFragment`, two statements — plus the composed `audit.append_fragment` when `requires_audit`). `guarded_write` = `authorize_guarded` + `fragments` + execute (ONE implementation) + `except TxnGovernedConflictError → raise GovernedConflict from error`. The Python `if row_count == 0` conflict branch DELETED (unreachable — the THROW fires first); on success the addressed record matched 0-or-1 by construction of `type::record`, so a successful guarded write is `row_count=1`.
- **`loremaster/memory/ledger.py`** (§3.2 / #441 / #453): `retire(memory_id, *, valid_until, superseded_by)` (merges the close ISO stamps into the row's JSON metadata so a rebuild replays it RETIRED, not revived) + `delete(memory_id)` (removes an orphan successor row on #441 compensation). Both are stdlib `sqlite3` + `json` on the existing table (no DDL change); a missing row is a no-op.
- **`loremaster/memory/local.py`**: `remember` order = `authorize_guarded` (deny-first, no effect) → ledger `record` FIRST (now carrying owner_principal/owner_agent/scope/created_at) → ONE `write_guard("remember")` around `self._apply([*plan.fragments(close_set), upsert(new_row)])` → on ANY exception, `ledger.delete(memory_id)` compensate + (if `TxnGovernedConflictError`) map → `GovernedConflict`, else re-raise. `invalidate` retires the ledger row AFTER the store close commits (store-first; mirrors the exact stamped `valid_until`). `_ledger_metadata` gains owner/scope/created_at keys. `_build_content` takes `created_at`/`valid_from` as PARAMETERS (not `now`) + owner/scope/valid_until/superseded_by, so replay reconstructs the ORIGINAL window; a legacy row with NONE owner/scope FAILS CLOSED. `_replay_record` reconstructs those stamps FROM the ledger and composes the same-owner predecessor-close `UPDATE … WHERE superseded_by IS NONE OR = $rc_succ` (idempotent) into the SAME replay txn — the NEW L1 literal that resolves the i-a `_REPLAY_CLOSE_ENTRY` ghost pin.
- **`loremaster/server.py`** (§3.3): `render_subject_bound(subject) -> Rendered` (DERIVED from the typed Subject: `scope: principal … · agent … · visible via N keep(s)`, `0` renders `visible via 0 keep(s)` — the bound is NAMED; ids route through `render_attributed`, the #321 containment seam) + `_render_recalled_memories(recalled, *, subject)` prepends it INCLUDING the empty case; `recall` passes `subject=`. No withheld count.

## §RED→GREEN (target modules — :18000, `-n0`)
- RED-at-HEAD is documented in the committed contract reports (contract-63b-i-b-3 §RED-AT-HEAD, contract-63b-i-b-4 §RED-AT-HEAD): the i-b modules were 27 failed/11 passed + 6 failed/3 passed at HEAD before this build; the new-API pins RED via unbuilt `authorize_guarded`/`TxnGovernedConflictError`.
- GREEN after this build: `test_memory_supersede_atomicity_63b_ib.py` + `test_memory_replay_fidelity_63b_ib.py` + `test_memory_render_bound_63b_ib.py` + `test_memory_f5_registration_63b_ib.py` + `test_surreal_store.py::TestDomainRejectionErrorType` + `test_retry_seam.py` + `test_memory_enforcement_63b_ia.py` (the i-a ghost-pin module) → **747 passed, 0 failed, 2 warnings in 45.03s**. The 2 warnings are pre-existing benign un-awaited-coroutine RuntimeWarnings in two error-path test fixtures (a raising-hook test and a subscription-retry test) — not caused by this build.

## §DRY-LEDGER (§6 — new/changed reusable symbols)
| symbol | disposition | note |
|---|---|---|
| `governed.authorize_guarded` / `GuardedPlan` | **EXTENDED** `governed.guarded_write` | the Python-leg split; `guarded_write` becomes its composition (design §6 reuse ledger, §2.2 step 3) |
| `governed.GuardedPlan.fragments` | HAND-ROLLED (method on the new plan) | emits the in-store LET/IF/THROW guard fragment; no prior emitter — the conflict guard moved from Python into the store |
| `_txn._domain_rejection_error` | HAND-ROLLED | the ONE label→type mapping helper both raise sites call (ROUTING-IS-NOT-SHARING); nothing pre-existing mapped a classified label to a typed subclass |
| `_txn.TxnGovernedConflictError` | HAND-ROLLED | mirrors the existing `TxnContentionExhaustedError` shape (subclass of `SurrealStoreError`); a new error class, nothing to reuse |
| `_txn._classify_engine_error` (marker branch) | **EXTENDED** the existing classifier | one membership branch beside `_ASSERT_VIOLATION_MARKER` — reuses the classifier, not a new one |
| `MemoryLedger.retire` / `MemoryLedger.delete` | HAND-ROLLED | two SQL statements on the existing table; the ledger had `record`/`all_records`/`count`/`close` only (no per-row update/delete) |
| `local._build_content` / `_ledger_metadata` | **EXTENDED** (same functions, new params) | save + replay already SHARE `_build_content`; widened to carry governance/lifecycle stamps as parameters |
| `server.render_subject_bound` | HAND-ROLLED | one derived-from-Subject line; reuses `render_line`/`render_attributed` (the #321 containment seam), does not fork them |
(The design doc §6 carries the authoritative Packages-considered + reuse ledger for all of these — cited, not re-derived here.)

## §FULL-SUITE (the CROSS-CUTTING gate — the #452 lesson, run for the first time on this reference)
- **First full-suite run (proven reference as-applied, before my B/C fixes):** `uv run python -m pytest -n auto` → **16 failed, 11108 passed, 51 skipped, 3 xfailed in 313.93s**. The scoped 80/0 reference (adversary-63b-i-b-2) + contract-4's fidelity-module run had NEVER run the full suite — exactly the regression-hiding the brief warned about.
- **BASELINE at HEAD 187201c (pre-build, restored from `/tmp/build63bib-pre-backup`):** the 16 failing node ids run against HEAD → **15 passed, 1 failed**. So **15 of the 16 are NEW regressions my build (the reference shape) caused**; **1 is PRE-EXISTING** (`test_secret_typing`, below). Tree re-applied to the build afterward and `diff -rq` verified byte-identical to the reference (then B/C fixed on top).

## §REGRESSIONS (the 15 NEW failures — 4 classes; root cause + disposition per CLAUDE.md scope law)
**Class A — render signature change (10 failures, OUT OF WRITABLE SET → STOP-AND-FLAG).**
`_render_recalled_memories(recalled)` → `_render_recalled_memories(recalled, *, subject)` (design §3.3, contract-required) + the NEW `render_subject_bound` render method break the GENERAL render-coverage/forgery instruments, which derive & drive EVERY `AppContext` render method:
- `test_link5_render_containment.py` (6): all `TypeError: AppContext._render_recalled_memories() missing 1 required keyword-only argument: 'subject'` — its `_served(method, *args)` harness (line ~1897) drives `_render_recalled_memories` positionally with no `subject`.
- `test_render_slot_inventory.py` (4): same root — its over-drive harness TypeErrors on `_render_recalled_memories`, cascading to `test_the_over_drive_actually_produced_observable_tokens` / `test_the_drain_row_task_id_slot_is_driven_with_content` / `test_every_served_field_is_driven_with_content_or_justified_safe` / `test_every_old_net_method_is_driven_with_forgery_content`.
These are coverage INSTRUMENTS, not the i-b contract. The design's i-b writable set (§5.1) named `server.py (recall render + render_subject_bound)` but did NOT anticipate that a subject-required render + a new render method break the general render-driver harnesses. **They are NOT in my writable set (the brief names only the 5 production files).**
- **RECOMMENDED FIX (for the lead / a fix-wave, in the render-instrument test files):** teach the render harness to (a) pass a forged `subject` (a `pdp.Subject`/`_forge`d Subject) when driving `_render_recalled_memories`, and (b) register `render_subject_bound` as a driven render (it is a new served render method — the #420 coverage-as-a-checked-variable idiom SHOULD grow to include it, and driving it with a forged `principal_id`/`agent_id` exercises the `render_attributed` neutralisation). This is exactly the render-coverage instrument's job; it is design-adjacent (how the general inventory treats the new render), so it wants the lead's routing, not a silent builder edit.

**Class B — `governed.py` dict `.update()` false positive (1 failure, FIXED — in writable set, disclosed).**
`test_governed_substrate_63a.py::test_governed_py_has_no_direct_sdk_call_site` flagged `update() at governed.py:378` — but that line was `fragment_params.update(params)`, a **dict** method the NAME-keyed R4 AST scanner mistook for a connection `.update()`. **FIX:** rewrote the merge as a dict literal `{"gw_id": …, **self.guard_params, **(params or {})}` (semantics-identical, no `.update(`). Re-run → GREEN. This is the classic "enumerate the forbidden by NAME" instrument weakness (CLAUDE.md instrument lesson) surfacing in the reference; the fix sidesteps it cleanly and keeps the R4 invariant intact.

**Class C — FP-06 durability vs §2.2-step-4 compensation (1 failure, FIXED in writable set + a design nuance FLAGGED).**
`test_memory_backend.py::TestDurabilityWriteThrough::test_ledger_row_persists_when_the_surreal_side_is_unreachable[real]` — a PLAIN remember against a DEAD Surreal URL must KEEP its durable ledger row (FP-06, the #131-class durability guarantee: the ledger is the copy a Surreal failure/wipe cannot touch; a later restore re-embeds it). The reference generalized the #441 compensation `try/except` to wrap ALL remembers, so a plain remember's transport failure ran `ledger.delete(memory_id)` — deleting the exact row FP-06 exists to preserve. **This is a genuine tension the design under-specified: §2.2 step 4 says "on ANY exception (… transport) … compensate", but §2.3 says "the ledger is … AUTHORITATIVE; the store CONVERGES to it by replay" and FP-06 pins ledger-survives-a-Surreal-failure.**
- **FIX (minimal, in `local.py`, disclosed as a deviation):** gate the compensation on `plan is not None` — compensation runs ONLY for the COMPOSED SUPERSEDE flow that §2.2 step 4 actually describes (the orphan-successor window), never for a plain remember. FP-06 GREEN + the full #441 atomicity module (incl. `test_a_failed_successor_create_compensates_the_ledger`, whose poison is a supersede) 14/0 GREEN.
- **Two readings I chose between (brief-base §2 — I picked the first, flagging the alternative):** (A, chosen) compensation is SUPERSEDE-scoped — the reference over-applied a supersede-flow step to plain remembers; gating on `plan is not None` is the faithful reading and preserves FP-06. (B, rejected) keep compensation on ALL exceptions and instead update the FP-06 test to expect deletion — this would SACRIFICE a load-bearing #131-class durability guarantee, so I rejected it.
- **RESIDUAL DESIGN NUANCE (flagged, not blocking):** even for a SUPERSEDE, a TRANSIENT (transport) failure now still deletes the ledger successor row, though restore could have re-applied it faithfully (create + the `_replay_record` predecessor-close). Design §2.3 already names this compensation window a LOW bound; a stronger form would compensate only on a PERMANENT domain rejection (ASSERT/coercion/`GovernedConflict`), never on transport. No test covers supersede+transient, so it is unpinned. **Fork for the design-sidecar/operator:** is supersede-scoped compensation (my fix) sufficient, or should it be narrowed to domain-rejection-only? I recommend confirming supersede-scoped for now (matches the design's stated LOW bound) and optionally pinning the domain-vs-transient distinction in a follow-up.

**Class D — stale `63a_iv` enforcement twin (3 failures, OUT OF WRITABLE SET → STOP-AND-FLAG).**
`test_memory_enforcement_63a_iv.py::TestEveryRawMemoryMutationIsAllowlisted` (3): `raw-mutation set moved. new (unadjudicated): [MutationSite(function='_replay_record', verb='UPDATE')]` + `UNCLASSIFIED raw memory mutation … not allowlisted` + `guarded_write consumers were not derived: ['invalidate'] … remember must be recognised as GUARDED` (`{'invalidate','remember'} <= {'invalidate'}`). Root cause: my `_replay_record` adds a legitimate NEW raw-mutation site (the #436/#453 predecessor-close UPDATE — the intended `_REPLAY_CLOSE_ENTRY` L1 literal), and `remember` no longer calls `guarded_write` directly (it calls `authorize_guarded` + composes), so `remember` drops out of the OLD guarded-consumer derivation.
- **This module is a STALE PRE-i-a TWIN.** Git: `63a_iv` was last touched by `0ea337b` (the 63a-iv wave); the 63b-i-a wave (`519c0bb`) created the RESHAPED instrument `test_memory_enforcement_63b_ia.py` (effect-based detection; `TestL1IsACoverageFloorWithADerivedGrammar`, `TestGuardedWriteIsAnAllowlistedLabelFrame`, `TestTheGovernedPopulationSetIsADerivedOutput`, …) which is GREEN with my build (it carries the i-b F5 allowlist DATA incl. `_REPLAY_CLOSE_ENTRY`, and recognises `remember`-via-`authorize_guarded` as guarded). `63a_iv`'s `TestEveryRawMemoryMutationIsAllowlisted` is the OLD instrument that certifies the pre-i-b raw-mutation/guarded-consumer world — CLAUDE.md's "tests written before a semantic change certify the OLD world; a green suite may be green because it still asserts the corpse."
- **RECOMMENDED FIX (for the lead / a fix-wave — the i-a wave appears to have left it behind):** either (1) DELETE `test_memory_enforcement_63a_iv.py::TestEveryRawMemoryMutationIsAllowlisted` (or the module) as superseded by `63b_ia`'s L1-coverage tests (my recommendation — it duplicates a now-reshaped instrument), or (2) update its allowlist DATA to adjudicate the new `_replay_record` UPDATE site and recognise `remember` as a guarded consumer. **Scope ambiguity (brief-base §2):** the design §5.1 grants i-b "the F5 allowlist DATA" but the brief's writable set names NO test files, and the i-b F5 DATA lives in `63b_ia` — so `63a_iv` is out of my grant. Needs the lead's ruling.

## §FLAGS (pre-existing / unrelated)
- **PRE-EXISTING (not my build): `test_secret_typing.py::TestOutgoingAuthHeadersGoThroughATypedSeam::test_secretstr_is_minted_only_where_a_credential_ORIGINATES`** fails at HEAD 187201c (baseline) because a CONTRACT-AUTHOR-committed probe, `scripts/probe_supersede_throw_rollback.py:110`, mints a `SecretStr` outside the allowed-origin allowlist. This is unrelated to my 5-file build (it is a probe script committed by contract-63b-i-b). It should be surfaced to whoever owns that probe (add the probe to the allowlist, or move the mint to an origin) — not in my writable set.
- **PRE-EXISTING (not my build): 3 typecheck errors in `test_memory_f5_registration_63b_ib.py:286/287/292`** (`ObservedEffect | None has no attribute row_deltas`) — identical at HEAD (16 errors/4 files at HEAD → my build resolves 13 by building the referenced symbols → 3 remain). A committed F5-registration contract file I did not touch (git-confirmed). Needs a `union-attr` narrowing (an `assert … is not None`) by its owner.

## §DISCIPLINE
- **Build method:** the proven 80/0 reference (adversary-63b-i-b-2, `/tmp/lore-adv-63bib2`, adversary-satisfied over 2 passes; contract-63b-i-b-4 discrimination proof) was applied to the 5 writable-set files after I read + verified the COMPLETE diff of every hunk against design §2/§2.5/§3/§3.3 + the atomicity contract. Post-apply `diff -rq` was byte-identical to the reference; then the full suite (which nobody had run on the reference) drove the B + C fixes on top.
- **Backups (content, standing law — mutating a tracked tree):** `/tmp/build63bib-pre-backup` (HEAD 187201c state of the 5 files) + `/tmp/build63bib-mybuild` (my build incl. B+C, used to restore after the HEAD typecheck baseline). The tree currently holds MY build (git-verified: only the 5 writable-set files modified).
- **Baselines established (verify, don't assume):** the 16 full-suite failures re-run at HEAD → 15 pass/1 fail (so 15 NEW, 1 pre-existing); typecheck re-run at HEAD → 16 errors/4 files (so the 3 remaining are pre-existing). Both via restore→run→re-apply, each re-apply `diff -rq`-verified byte-identical.
- **No git mutation** (the lead commits). **No scratch-copy mutation proof needed** (I built in the real tree with content backups; `loremaster.__file__` provenance is moot — no scratch reference build authored by me). **No sub-agents spawned** (INLINE per brief). **Writable set respected:** only `governed.py`, `store/_txn.py`, `memory/ledger.py`, `memory/local.py`, `server.py` modified; every other failing file left UNTOUCHED and FLAGGED.
- **`_txn.py` SCOPED grant honored:** only the §2.5 symbols touched (`TxnGovernedConflictError`, `_ERROR_CLASS_GOVERNED_CONFLICT`, `_GOVERNED_CONFLICT_MARKER`, the `_classify_engine_error` marker branch, `_domain_rejection_error` + its 2 call sites). `_rollback_verdict`, the retry driver, cascade markers, envelope checks — untouched.
- Tests ran against the TEST store `:18000` only (never :18500).
