brief-base v14 read
brief project v7 read

# REPORT-adversary-63b-i-a — CONTRACT ADVERSARY, packet 63b wave i-a (F5 runtime root-fix)

STARTED: 2026-09-03 — adversary grading the RED contract at HEAD 519c0bb BEFORE any builder.

## SUMMARY BLOCK
- receipt: brief-base v14 read · brief project v7 read
- state: done
- **VERDICT: CONTRACT INSUFFICIENT** — 2 concrete MISSING PINS + 1 borderline-C-DEF oracle fragility (all below). Otherwise a strong, satisfiable contract: every OTHER guard/leg discriminates a wrong build, proven empirically.
- **P1 headline: 2 wrong builds SURVIVE the contract** — (a) a classifier that blesses ANY labeled write WITHOUT its entry's effect predicate → 30/30 GREEN; (b) `governed_populations` HARDCODED to `{memory}` → GREEN. Both reproduced in scratch.
- **perf-finding: the prior hang is a REFERENCE-BUILD BUG (observer re-entry via `.query`→patched `.query_raw`), CLOSED — NOT a design flaw** (filed lore #454). Fix: observe at `query_raw` ONLY + read via unwrapped query_raw.
- **C-DEF satisfiability: 57 passed / 0 failed** vs a correct reference (new module 30/30 serial 9.26s NO HANG · 63a_v+63a_iv+bounds 27/27 · ruff clean incl. the R4-deletion harder-leg). Reference required 6 pin-forced fixes (good discrimination).
- Reuse ledger: none committed — I am read-only/advisory; every symbol I wrote lives in the DISPOSABLE scratch reference (`/tmp/adv63bia-scratch`, scratch_copy) and is the builder's to author. The committed contract's own reuse ledger (REPORT-contract-63b-i-a) covers the substrate shapes.
- Graded: 519c0bb · HEAD-at-report: 519c0bb · SAME
- Packages considered: none new — the reference is bespoke test substrate (stdlib `ast`/`re`/`asyncio`/`contextvars` + the SurrealDB SDK's own `query_raw`); P-PKG diff vs the author's table below (agree: `bespoke`/`keep_with_trigger`).
- decisions-needed: F-TRAP-3 (dotted `embedding.*`/`labels.*`) fix choice is the author's (3 options given); the 2 missing pins are the author's to add.
- pointers: missing pins §"MISSING PINS"; P1b §"QUANTIFIER TABLE"; P1c §"REACH TABLE"; receipts §"P1 WRONG-BUILD MATRIX" + §"SATISFIABILITY RECEIPT"; traps F-TRAP-1/2/3; gotchas A/B/C; lore #454.

## MISSING PINS (the author must add these before a builder starts)
1. **The classifier must ENFORCE a label frame's effect predicate (P1b quantifier gap).**
   - *Test that should exist:* a pure-unit pin that constructs `ObservedWrite(label="_reinforce", effect=<an ObservedEffect whose changed_columns include a GOVERNED column, e.g. {"importance","scope"}>)` and asserts `classify_tree_observed_write(write, MEMORY_TREE_ALLOWLIST) is False` (with a positive control: the same label + a `{"importance"}`-only effect classifies True). Generalise it ∀ label frame that carries an effect predicate — a labeled write whose effect FAILS the entry's predicate must be UNCLASSIFIED.
   - *Defect it catches:* a classifier that keeps the HEAD 2-leg behaviour `if observed.label is not None: return True` (or a 4-leg rewrite whose LABEL leg returns True without calling `entry.effect`). MEASURED: such a build passes ALL 30 pins (`WB-label-ignores-effect`). This silently defeats §1.4's "effect pins REPLACE text-shape pins for labels" and re-widens the #138/R2 hand-set-label bound the design set out to NARROW — a `_reinforce`/`ensure_ready`/`remember` frame that also seizes a governed column would launder.
2. **The governed-population derivation's REACH must be a CHECKED VARIABLE (P1c reach gap — the fifth-defeat lesson, reproduced inside the population instrument).**
   - *Test that should exist:* a pin that proves `governed_populations()` is DERIVED, not a literal — e.g. parametrise it with a synthetic `surreal_schema` source (as the whole-tree scan's fixture-tree discriminator does) in which a NEW `_<t>_statements` calls `_governed_field_specs`, and assert the derived set GROWS to include `<t>` (and, dually, a governed-endpoint relation table joins). A mutation-proof form: adding a `_governed_field_specs` caller to a synthetic schema must change the output.
   - *Defect it catches:* `def governed_populations(...): return frozenset({MEMORY_TABLE})` — a hardcoded literal. MEASURED: it passes `test_the_derived_population_set_is_the_known_set` (which only asserts `== {memory}`). So the design's rider §1.6-vii ("a fixture DDL slice calling `_governed_field_specs` for a new table → the set grows → RED") has NO committed instrument. A hardcoded set means message/agent would never be detected as needing an F5 case — exactly the "reach as a checked variable over the SET of populations" §1.5c-iii claims to enforce.

## BORDERLINE C-DEF — the ensure_ready oracle is FRAGILE (F-TRAP-3, restated as a verdict item)
`test_the_rebuild_arc_is_observed_and_classified` CANNOT pass unless the builder's observer independently normalises `INFO FOR TABLE` object names to top-level. `INFO FOR TABLE memory` reports array-element field defs as `embedding.*`/`labels.*` (measured), but `_effect_ensure_ready` compares against `_memory_ddl_object_names()`, whose regex stops at `.` → `embedding`/`labels`. So a correct ensure_ready effect fails `touched <= managed`, and the failure message misleadingly says "the ensure_ready re-DEFINE was NOT observed / DDL apply unattributed". FIX (author's choice): (a) make `_memory_ddl_object_names()` emit the `.*` element names too; or (b) top-level-normalise BOTH sides in `_effect_ensure_ready`; or (c) SPECIFY the observer normalisation + add a pin. This is the #131/#107 class ("the test environment is a fiction"): the oracle was written against the DDL STRING, never against what the ENGINE reports.

## REFERENCE BUILD — FINDINGS (measured in scratch /tmp/adv63bia-scratch, provenance-asserted; loremaster.__file__ = /tmp/adv63bia-scratch/loremaster/loremaster/__init__.py)

The reference build implements design §1.2–§1.5 + §5.1 Q1 (sdk_guard hook seam; governed_exempt(name,*,statement)+origin capture; ensure_ready write_guard; derived grammar/keywords/populations; effect observer; 4-leg classifier). Building it surfaced BUILDER TRAPS in the DESIGN's wording — each correctly CAUGHT by a committed contract pin (so the pin is GOOD; the finding is that a builder faithfully following the design's example wastes a cycle on a self-inflicted RED). These are for the lead/builder, NOT contract-insufficiency (the pins fire).

- **F-TRAP-1 (population derivation wording).** §1.5c-iii says the set is "walked from the generate_*_ddl output (**owner_principal field definitions** + TYPE RELATION…)". A builder implementing that literally (any table with an `owner_principal` DEFINE FIELD) gets `{memory, agent, briefed, to}` — because `agent.owner_principal` is a **packet-62 DIRECT field, not emitted via `_governed_field_specs`**. `test_the_derived_population_set_is_the_known_set` then REDs (expects `{memory}`). The satisfiable derivation keys on **`_governed_field_specs` CALLERS** (AST over surreal_schema): only `_memory_statements` calls it at HEAD → `{memory}`. RECOMMEND: reword §1.5c-iii / add a one-line builder note: "derive from `_governed_field_specs` callers, NOT from `owner_principal` presence (agent's is a 62 direct field)."
- **F-TRAP-2 (§1.4 module-constant is an L1 orphan).** §1.4's own example is `principals.MIGRATE_MEMORY_SCOPE_STATEMENT = f"UPDATE {MEMORY_TABLE}…"` — a MODULE-LEVEL constant. The widened L1 whole-tree scan derives that literal at `<module>` scope → `TreeMutationSite(principals.py, <module>, UPDATE)`, which no frozen allowlist entry covers (the entry is keyed `_migrate_memory_scope`) → `test_every_derived_site_is_in_a_site_backed_entry_and_no_ghost` REDs (orphan + ghost). The builder CANNOT edit the frozen allowlist. The satisfiable shape is a **FUNCTION-LOCAL** statement var inside `_migrate_memory_scope` (still ONE source, passed to both governed_exempt and run_query) → derives at `_migrate_memory_scope` → matches the entry. Root cause: the L1 AST scan attributes a raw-mutation literal to its DEFINING scope, not its EXECUTING scope. RECOMMEND: §1.4 must say function-local (not `principals.MIGRATE_…`); OR the L1 scan attribute a module-const statement to its using function; OR the contract note the constraint. (The contract's `_MIGRATE_GOLDEN` docstring currently TELLS the builder to define `principals.MIGRATE_MEMORY_SCOPE_STATEMENT` — the exact trap.)

- **F-TRAP-3 (dotted array-element fields break `_effect_ensure_ready` — the sharpest finding).** `INFO FOR TABLE memory` reports SurrealDB **array-element field definitions** as `embedding.*` and `labels.*` (measured, probe3). But the contract's `_memory_ddl_object_names()` parses the DDL STRING with a regex `([A-Za-z_][\w:]*)` that stops at `.`, so it captures `embedding`/`labels` (NOT `embedding.*`/`labels.*`). Therefore `_effect_ensure_ready`'s `touched <= managed` is `{…,'embedding.*','labels.*'} <= {…,'embedding','labels'}` = **False** → a CORRECT ensure_ready DDL frame does NOT classify → `test_the_rebuild_arc_is_observed_and_classified` REDs on a correct build. Satisfiable ONLY if the builder's observer NORMALISES `INFO FOR TABLE` names to top-level (`key.split('.')[0]`), which the contract never specifies. This is the same #131/#107 class the repo law names ("THE TEST ENVIRONMENT IS A FICTION"): the oracle was written against the DDL string, never against what the ENGINE actually reports. RECOMMEND (pick one): (a) `_memory_ddl_object_names()` accounts for `.*` element fields (add the element defs), OR (b) the contract SPECIFIES the observer normalises schema names to top-level and adds a pin asserting it, OR (c) `_effect_ensure_ready` compares top-level-normalised sets on BOTH sides. Without one of these, the builder ships a naive observer, greens every OTHER pin, and the rebuild arc reds for a reason that looks like "the DDL frame is unbuilt" when it is actually a naming mismatch. ⚠ This is a genuine contract fragility, borderline C-DEF: the pin as written cannot pass unless the builder independently discovers the `.*` normalisation.

## OBSERVER / RE-ENTRANCY — BUILDER GOTCHAS (needed for a CORRECT, non-hanging observer)
- **GOTCHA-A (THE HANG ROOT, measured — react to `query_raw` ONLY, not `.query`).** The observer must observe at the **`query_raw`** door alone. `.query()` DELEGATES to `.query_raw()` internally (SDK 2.0.0), and the guard patches BOTH. If the dispatcher fires on a `.query` call, then awaiting that call's coroutine runs unwrapped `.query`, whose body calls `self.query_raw(...)` = the PATCHED door → the dispatcher fires AGAIN → `_observed()` → `async with lock` while the OUTER `_observed` (for the `.query` call) STILL HOLDS the lock → **DEADLOCK** (non-reentrant `asyncio.Lock`). MEASURED: my first reference (dispatcher on `{query, query_raw}`) HUNG on the member/migrate battery (both use `.query` via run_query); reacting to `query_raw` ONLY completed cleanly. `query_raw` is the narrow waist every statement passes through (harness `COUNTABLE_DOORS` proves select/create/insert/upsert/update/delete all route through it), so observing there catches everything with NO re-entry and NO double-count. ⚠ The design §1.8's `event.original` guidance is INSUFFICIENT — the observer additionally (a) reacts to query_raw only, and (b) reads before/after via the UNWRAPPED query_raw (`type(conn).query_raw.__wrapped__`), because reading via `.query` (or `event.original` when it is `.query`) re-enters the same way.
- **GOTCHA-B (persistent dispatcher, not per-observe append).** The detach pin (`test_detaching_the_hook…`) CLEARS `_sdk_guard._CALL_HOOKS` and THEN enters `observe_governed_table_writes`. If `observe_…` appended its own hook, it would re-arm after the clear and the pin would RED on a correct build. The satisfiable design: ONE persistent dispatcher hook appended at `_governed_contract` import; `observe_…` only registers `(table, list)` into a registry the dispatcher consults. Clearing `_CALL_HOOKS` then truly disarms (design §1.3 "reach is a checked variable via _CALL_HOOKS"). RECOMMEND: §1.8 should say the hook is a persistent dispatcher + a registry, not appended per observe. VERIFIED: `test_detaching_the_hook…` GREEN with this design.
- **GOTCHA-C (origin capture at exempt CONTEXT-ENTRY, skip `contextlib.py` by FILENAME).** Leg-4 origin (`_migrate_memory_scope`) cannot come from a stack walk at SDK-call time (the store driver `_txn.py::_attempt` is always the immediate caller). `governed_exempt` must capture the entering frame — walking up past `governed.py` AND `contextlib`. ⚠ The `contextlib` frame's file is `/usr/lib/pythonX/contextlib.py`, so a `"contextlib" in path.parts` check FAILS (the part is `contextlib.py`, not `contextlib`) and the origin resolves to `contextlib.__enter__` → leg-4 REDs. Skip by `path.name == "contextlib.py"`. (Reference-build bug I hit; the pin correctly caught it.)

## PERF / RE-ENTRANCY DETERMINATION (the prior-hang finding) — RESOLVED
**DETERMINATION: the prior hang is a REFERENCE-BUILD BUG (dispatcher re-entry via the `.query`→`.query_raw` delegation, deadlocking on the observer's held non-reentrant lock), CLOSED. NOT a design flaw.** Root + fix measured (GOTCHA-A): observe at `query_raw` ONLY + read via the unwrapped `query_raw`; the member battery + migrate + rebuild arc + gather-of-two then all complete in seconds, serial. The design's own re-entrancy concern (§1.5 "8-way contention batteries whose semantics the serialising lock would change") does NOT arise: the observer is armed ONLY inside F5 battery blocks (single-frame + the gather-of-two, §1.6-iv), NEVER during any N>2 contention battery. `rebuild_embeddings`'s non-reentrant `_rebuild_lock` spans `_recreate_memory_table`→`ensure_ready`→`restore_from_ledger` (internals UNLOCKED); the observer's raw-query_raw reads bypass `_query`/`_rebuild_lock` → no cross-lock deadlock. Per-call diff is O(rows); battery fixtures ≤1 row → not O(n²)-hot. ⇒ does NOT block. Filed as a builder gotcha (see lore_findings).

## SATISFIABILITY RECEIPT (C-DEF) — measured at scratch = HEAD 519c0bb reference build
- Scratch: `/tmp/adv63bia-scratch` (scratch_copy.sh, provenance-asserted). `loremaster.__file__ = /tmp/adv63bia-scratch/loremaster/loremaster/__init__.py` (#140 receipt).
- Reference implements design §1.2–§1.5 + §5.1 Q1 per REPORT-contract A–E, WITH the 4 corrections the pins forced (all reference-build fixes, NOT contract edits): governed_populations via `_governed_field_specs` callers; function-local migrate constant; observer reacts to `query_raw` ONLY + reads via unwrapped `query_raw`; observer schema names normalised to top-level; origin skip by `contextlib.py` filename; #138 docstring preserved on the classifier.
- **NEW MODULE `test_memory_enforcement_63b_ia.py`: 30 passed, 0 failed — SERIAL (`-n0`), 9.26s (NO HANG).** Bounded `timeout` used; passed-count in tail confirmed.
- **ruff harder-leg** (reference-changed prod + the R4-pin-deleted contract files): `All checks passed!` — the R4 deletions left no orphaned imports.
- **63a_v + 63a_iv + bounds_63a_iv: 27 passed, 0 failed** (surviving pins hold; the instrument replacement did not break them — after the #138 docstring was preserved on the classifier: 63a_v's `TestTheHandSetLabelBoundIsNamedInTheInstrumentDocstring` CORRECTLY red until it was, a discriminating pin).

## P1 WRONG-BUILD MATRIX (measured in scratch; restore byte-exact vs /tmp/adv63bia-refbak/MD5SUMS confirmed)
| wrong build | target pin | result |
|---|---|---|
| classifier DROPS leg 2 (golden text) | `test_leg2_reds_a_widened_statement_with_the_same_effect` | **CAUGHT** (1 failed) |
| classifier DROPS leg 3 (effect) | `test_leg3_reds_a_golden_edited_to_bless_a_seizure` | **CAUGHT** |
| classifier DROPS leg 4 (origin) | `test_leg4_reds_a_borrowed_token_from_a_foreign_origin` | **CAUGHT** |
| grammar drops CREATE/INSERT/RELATE | `test_the_derived_grammar_covers_the_exotic_write_verbs` | **CAUGHT** |
| observe_ RE-ARMS hook per-call (reach not checked) | `test_detaching_the_hook_reds_every_runtime_observation…` | **CAUGHT** |
| observer DROPS a runtime_observed frame (ensure_ready) | `test_every_runtime_observed_frame_is_observed_with_a_nonempty_effect` | **CAUGHT** |
| classifier LABEL leg ignores entry.effect (returns True on any label) | (none) | **SURVIVES — MISSING PIN #1** (30/30 green) |
| `governed_populations` HARDCODED `{memory}` | `test_the_derived_population_set_is_the_known_set` | **SURVIVES — MISSING PIN #2** (green) |

## P1b — QUANTIFIER TABLE (each §1 invariant: ∀-over-inputs vs guarded; guarded rows carry a receipt)
| invariant | ∀ / guarded | receipt |
|---|---|---|
| §1.2 detection = state-diff effect, ∀ observed governed write carries non-empty effect | ∀ | `test_every_observed_member_write…` + `test_a_no_effect_write…` (empty→not event) GREEN vs ref; a verb-keyed/None-effect build reds them |
| §1.2 verb-agnostic (CREATE/INSERT/RELATE/concat all observed) | ∀ over SurrealQL verbs (via query_raw) | `test_detection_is_verb_agnostic…` + `test_ii_shape_agnostic…` CAUGHT a grammar/verb-keyed build |
| §1.2 no-effect / rollback / write-then-revert NOT observed | ∀ (with anti-vacuity controls in-pin) | pins GREEN vs ref; each has a store control proving state did/didn't move |
| §1.4 exempt classified iff name∧golden∧effect∧origin | ∀ (legs 2/3/4 independent) | leg2/3/4 wrong builds all CAUGHT (matrix above) |
| **§1.4 LABEL classified iff entry.effect holds** | **guarded — evaluated only where the effect is CORRECT** | **NO receipt kills it: `WB-label-ignores-effect` SURVIVES → MISSING PIN #1.** Every battery label frame has a correct effect, so the ∀ is never exercised against a wrong-effect labeled write |
| §1.5c-ii coverage: ∀ runtime_observed frame observed w/ non-empty effect | ∀ (checked variable) | `WB-coverage-drop-ensure_ready` CAUGHT |
| §1.5c-i L1 deny-by-default: ∀ derived site ∈ one entry | ∀ | the `(principals.py,<module>,UPDATE)` orphan from a module-const was CAUGHT |
| **§1.5c-iii population set DERIVED, ∀ new governed table joins** | **guarded — only the value {memory} is pinned, not the derivation** | **`WB-population-hardcode` SURVIVES → MISSING PIN #2** |
| §1.6-iv serialised attribution (gather-of-two, own per-call effect) | ∀ over the 2 gathered writes | `test_iv_two_gathered_writes…` GREEN vs ref (observer lock serialises; smear check present) |

## P1c — REACH TABLE (each guard/scan the contract introduces or relies on)
| instrument | reach DERIVED? | coverage a CHECKED variable? | effect vs proxy | one-source / mutation | leg run |
|---|---|---|---|---|---|
| L1 whole-tree raw-mutation scan | DERIVED (member roots from `[tool.uv.workspace] members`) | YES — new site → orphan → RED (measured: `<module>` orphan) | EFFECT (AST over real source) | shared `_raw_mutation_of_table`/`_statement_shape` | EMPIRICAL |
| derived keyword grammar / `SURREALQL_STATEMENT_KEYWORDS` | DERIVED from `surrealdb-docs` corpus stems (currency pin re-derives) | YES — `test_keyword_currency` re-derives & compares; `test_every_mutation_keyword_has_an_operand_rule` | EFFECT | one derivation | EMPIRICAL (grammar-drop CAUGHT) |
| effect observer reach = `_sdk_guard` hook | DERIVED (SDK class, all doors) | YES — detach → `require_observations`/empty → RED (measured) | EFFECT (state diff) | one `_CALL_HOOKS` | EMPIRICAL |
| §1.5c-ii frame coverage | DERIVED (∀ runtime_observed entry) | YES — frame-drop → RED (measured) | EFFECT | — | EMPIRICAL |
| **§1.5c-iii governed-population set** | **INTENDED derived, but NOT pin-enforced** | **NO — hardcode `{memory}` passes (measured)** | value-only | — | EMPIRICAL → **MISSING PIN #2** |
| **§1.4 label-frame effect predicate (classifier use)** | n/a | **NO — label-ignores-effect passes (measured)** | **PROXY: pins only check the entry HAS an effect (`test_the_label_frames_are_excluded…`), never that the classifier RUNS it** | — | EMPIRICAL → **MISSING PIN #1** |
| exempt 4-leg classifier | n/a | YES (legs 2/3/4 wrong builds caught) | EFFECT | shared classifier | EMPIRICAL |

## P-PKG (my table, diffed vs the author's §"Substrate-shape"/REPORT-contract P-PKG)
No NEW mechanism is specifiable via a package here — the instrument is bespoke test substrate over the live store. I independently built: the effect diff (stdlib dict-by-id + `_cmp_value`), schema diff (INFO-FOR-TABLE key sets), the SDK-class hook (stdlib), keyword derivation (stdlib `re`/`Path` over the corpus), grammar (stdlib `re`). Every one is `bespoke` for the same reason the author gave (a test-substrate observer; no PyPI SurrealQL parser is needed because detection is by EFFECT, not statement classification). The engine-native change-capture alternative (`DEFINE EVENT`/`CHANGEFEED`) is correctly `keep_with_trigger` (needs test-side DDL on the real governed table, unprobed co-residence with HNSW/RELATION — a governed table too large to snapshot is the trigger; none today). **Diff vs author: AGREE on every row.** No `bespoke` verdict rests on an unread API (I read the SDK's query/query_raw delegation live via the `__wrapped__`/re-entry behaviour, and the harness `COUNTABLE_DOORS` source-derivation).

## SUBSTRATE-SHAPE VALIDATIONS (brief-requested — both DISCRIMINATE)
- **(a) `effect: Callable[[ObservedEffect], bool]` (whole effect, not per-RowDelta).** VALIDATED: `_effect_ensure_ready` returns False when `row_deltas` are present (`test_the_ensure_ready_effect_predicate_discriminates` GREEN vs ref; the `moves_rows` case → False). So a DDL frame that MOVES rows is correctly rejected — the schema-delta predicate discriminates a row-moving DDL frame. The one-field-serves-both decision is sound. (Caveat: it is only satisfiable once the observer top-level-normalises schema names — F-TRAP-3.)
- **(b) `l1_site=False` excludes a DDL/label frame from the L1 ghost-check.** SAFE for i-a: the label-frame set is PINNED exactly `{ensure_ready, guarded_write}` (`test_the_label_frames_are_excluded_from_l1_and_carry_an_effect`), and the allowlist is FROZEN contract DATA the builder cannot edit — so a REAL raw-literal L1 site cannot hide behind `l1_site=False` (adding a third l1_site=False entry moves the pinned set → RED; and a real raw literal is derived by the L1 scan → orphan → RED anyway). No wrong build found that hides an L1 site behind the flag.

## P3 branch reachability / P5 fakes / P6 corpse-sweep (brief-scoped notes)
- P3: every changed branch is reachable through the real entry points (member battery, migrate_governed, rebuild_embeddings) — proven by the 30/30 receipt exercising them; the two UN-pinned branches are the two MISSING PINS above (a label-effect-false branch and a population-growth branch that no test reaches).
- P5: the substrate uses NO test doubles for the effect path — it drives the REAL store (:18000) and the REAL SDK guard; the only fake is `FakeEmbedder` (dim-8 deterministic vectors), which cannot bless the classifier/observer logic. The `_sdk_guard` hook is exercised live (a raising hook propagates — `test_a_raising_hook_is_loud` GREEN vs ref).
- P6 corpse-sweep: the contract DELETES the R4-a/R4-c bound pins, the 2-leg classifier controls + observer-patch-set meta-pin (63a_v), and the L2b label battery (63a_iv) — I confirmed 63a_v/63a_iv are 27/0 GREEN with those deletions AND that the surviving STANDS pins still fire (the #138 docstring pin correctly reddened my reference until preserved). No corpse (retired-shape assertion) survives in the migrated modules that I could find; the widened grammar did not resurrect a retired verb-regex assertion.



## PROGRESS LOG
- P0: registered on lore_comms (session packet63b), idle-gate contract written, report stub created. Reading role spec + design §1 + contract next.
- P1: READ IN FULL — role spec (contract-adversary.md), REPORT-contract-63b-i-a (ref-build shape A–E), design §1 (1.1–1.8), §5.1 ADDENDUM (Q1/Q2), §2–§5, and the whole contract module test_memory_enforcement_63b_ia.py (1289 lines, all pin families). Contract is dense + strong on first read: 4-leg exempt, effect predicates, coverage-as-checked-variable (§1.5c-ii), population derivation, DDL frame. Building mental model of the reference build. Reading substrate (_governed_contract.py, _sdk_guard.py) next to know stub shapes for the scratch reference.
- ORIENTATION NOTES (to attack): (a) contract IMPORTS MEMORY_TREE_ALLOWLIST from itself + defines effect predicates locally — these are TEST-side oracles, correct home. (b) §1.5c-ii coverage pin was added AFTER refbuild worker spawned (contract author flag) — must re-verify against my reference. (c) the observer perf/re-entrancy finding: prior scratch hung on store-heavy tail; my reference MUST use event.original (§1.8) to avoid guard re-entry deadlock. (d) coverage pin uses `expected |= {entry.exempt_name} if ... else set(entry.frames)` — need to check guarded_write frame observed under invalidate battery.
- P3 (DONE): built the full reference in scratch, ran the C-DEF receipt (57/0), determined the hang = ref-build bug (filed #454), ran the P1 wrong-build matrix (2 missing pins found), byte-exact restore verified. VERDICT: INSUFFICIENT. Report complete.
- P2 (substrate + guard + prod symbols read): CONFIRMED at HEAD — remember/_reinforce/_replay_record/_recreate_memory_table/guarded_write all wrap in write_guard; ensure_ready does NOT (i-a's patch). migrate_governed(memory)→_migrate_memory_scope. Harness creds root/spikeroot. Scratch copy /tmp/adv63bia-scratch READY + provenance-asserted.
- ⚠ MAJOR SATISFIABILITY SUBTLETY FOUND (to settle by building): in the hook model the observer fires at the SDK CLASS level with the store driver (loremaster/store/_txn.py run_query/execute_transaction) ALWAYS interposed between the business frame and the SDK call. So a stack walk from the hook lands on _txn.py, NOT on _migrate_memory_scope → leg-4 origin (`origin_site == (_PRINCIPALS,'_migrate_memory_scope')`) is UNSATISFIABLE by stack walk. The clean satisfiable design: capture the origin frame at governed_exempt CONTEXT-ENTRY (where the business frame IS the immediate caller). This is (at least) a builder gotcha; possibly a governed.py writable-set widening beyond "exempt API only". Building to confirm.
- ⚠ POTENTIAL C-DEF (to settle by building): _effect_ensure_ready compares observed INFO-FOR-TABLE object names ⊆ _memory_ddl_object_names() (regex over the DDL string, stops at '.'). If memory has dotted/nested fields (e.g. source.kind) and the observer keys SchemaDelta on raw INFO keys, the predicate reds even on a correct build. Building to confirm.
- ⚠ RE-ENTRANCY (design flaw vs ref-build-bug determination): confirmed rebuild_embeddings holds a NON-reentrant _rebuild_lock across _recreate_memory_table→ensure_ready→restore_from_ledger (internals run UNLOCKED). Observer reads via event.original (raw SDK, bypass _query) so no _rebuild_lock deadlock. Observer's OWN asyncio.Lock: if the before/after reads used the WRAPPED door they'd re-enter _guarded→hook→observer→re-acquire the held non-reentrant lock → DEADLOCK (== the prior scratch hang). event.original avoids it. Observer is armed ONLY in the F5 battery (single-frame + gather-of-2), NEVER an 8-way contention battery, so §1.5's "serialising lock changes N-way semantics" concern does not arise in this contract. Building to confirm the hang is a ref-build bug, CLOSED.
