# Packet 11-i — the 23 decisions, RULED

**Ruled 2026-07-25 by `lead-11i-b`.** Supersedes `STATE-2026-07-25.md` §3 as the decision record.

> **Operator, 2026-07-25:** *"You're crazy if you think I'm answering 23 decisions."*

Correct, and the mistake was mine in a specific way worth naming: **the operator issued decision
PRINCIPLES and the lead kept returning individual instances for adjudication.** Nearly every one of
the 23 was already determined by a directive given earlier the same day. Collecting them into a
numbered list made them *look* like open questions; they were applications.

**So: all 23 are ruled below, each with the principle it derives from.** Where a ruling rests on the
lead's judgement rather than a stated directive, it is marked **[LEAD CALL]** and listed in §1 — that
is the short list, and it is the only part worth the operator's attention.

## The standing directives these derive from

| id | operator directive, 2026-07-25 |
|---|---|
| **P1** | *"Add any package that does the job… I don't care how big the container gets… put them in rather than coding around the missing dependency."* |
| **P2** | *"Don't add code nodes to make it sort of work, only to have to rip the code out later… Do it right the first time."* |
| **P3** | *"This needs to be self maintaining. As the container runs, it should handle its own maintenance."* |
| **P4** | *"I don't care how it works, only that it works. And that it works on k8s."* |
| **P5** | *"We must support branches and worktrees"* — fix the hole, as with git in the container. |
| **P6** | *"The consumers of lore are agents, not humans. What does an agent want? Ask them."* |
| **P0** | The four §0 rulings (split at the store/runner seam · the R2 vehicle · adversary-before-ruling · 10-d stays undeployed). |

---

## 1. THE ONLY THREE WORTH YOUR ATTENTION — [LEAD CALL], veto cheaply

These three do NOT follow from a stated directive. Each is a judgement I made so the packet can move;
each is cheap to reverse now and expensive later, so if any looks wrong, say the number.

**L1 — decision 2, the ≥98% gate's sample population: RULED R-PACKAGE** (answered union + identifier +
absent leg, anchored excluded). The two readings differ by 4.1 points against a 2% budget, so this is
load-bearing. Reasoning: the verdict the gate governs is *decided on* the absent leg, so excluding it
measures agreement over a population the decision does not range across. **Re-open trigger:** R2's real
data — if the anchored-excluded count is large enough that the denominators diverge materially, revisit
before 11-ii.

**L2 — lease tunables: RULED UPSTREAM DEFAULTS (15s / 10s / 2s), not the sidecar's 60/20/5.** This
overrides the design sidecar. Reasoning: P1's *rationale* is that a package is better tested — and its
defaults are part of what is tested. `client-go`'s values are the ones running at scale everywhere. A
divergence should be earned by a measurement, and none exists. **Re-open trigger:** a measured
renewal-failure rate under real store latency.

**L3 — decision 21, BCa: RULED NOT ADOPTED** (kept as a named contingency). Reasoning: adopting it
would amend D8's pre-registered "central 90% percentile" — a statistical change with no evidence
demanding it, and pre-registration exists precisely so the method is not chosen after seeing data.
**Re-open trigger:** the percentile interval failing a coverage check on R2's real distribution.

---

## 2. RULED BY DIRECT DERIVATION — no operator attention needed

| # | decision | ruling | derives from |
|---|---|---|---|
| 19 | adopt `scikit-learn` | **ADOPT** | **P1**, verbatim |
| 22 | take the `kubernetes` dependency (21 pkgs, one 183-LOC module) | **TAKE IT** | **P1** — *"I don't care how big the container gets"* answers this exactly |
| — | numpy / scipy adoption record | **ADOPT** | **P1** |
| 11 | F9 bootstrap implementation | **VOID** — replaced by R3+R9 (`scipy.stats.bootstrap` + `roc_curve`, equality-proven) | **P1** |
| 20 | which implementation is production | **THE ORACLE** — roc-path as production, `choose_cosine_floor` as executable spec/oracle | **P2** — keeping an O(N²) path on the hot line is the code-node-to-rip-out shape |
| 13 | the lease | **`kubernetes.leaderelection` + SurrealDB lock adapter** | **P1** + **P4**; verified best-available three ways (source, web, upstream intent) |
| 23 | `release_if_held` | **ADD IT** | **P4** — without it every rolling update stalls maintenance for a full `lease_duration`; that is "does not work well on k8s" |
| 12 | §B vehicle provenance | **INTERIM** path-identical mount + fail-loud-on-null; **END-STATE** lore-owned worktree root + real git identity. No git bump. | **P5** + measured M1/M3 (§0.5) |
| 8 | C8 narrowed store projection inside `DEPLOY: no` | **AUTHORIZE** as an 11-i-a addition | **P0** — `DEPLOY: no` means *no served byte changes*; a new **unserved** read changes none. Ruling to intent, not to letter. |
| 4 | F5 cause enum + two-degeneracy split | **ADOPT** | **P6** — the consult ruled degeneracy renders as a *corpus property*; the split exists so an instrument artifact is never projected into that sentence |
| 5 | F6 `head_identity(axes)` + `query_shape` reserved | **ADOPT** | **P2** — default-eliding encoding means a future axis costs zero migration; the alternative is a rip-out |
| 1 | F1+F2 adopted-N rule + calibrated bound | **ADOPT** | engineering; measured twice blind (non-monotonicity), adversary-graded |
| 3 | F4 states + the 30–49 join | **ADOPT** | engineering; restores a predicate D0 removed |
| 6 | F7 pins, including the explicit drop | **ADOPT** | engineering; the dropped pin was *refuted* by the probe, not merely unneeded |
| 7 | F8's eleven surviving adjudications | **ADOPT** | **P0** — the adversary pass the operator ordered has run; these are its graded output |
| 9 | C11 manifest size bound | **ADOPT** | engineering; measure-then-tune with a named decision point |
| 10 | C13 bar re-registration | **ADOPT** | engineering; legitimate measure-then-tune deferral, decision point named at R2 review |
| — | layout inventory · R5 extraction plan | **ADOPT** | **P3** — the maintenance loop is extracted before its third instance exists |

---

## 3. What the operator still genuinely owns

Not decisions in the list above — these are scope, and scope is the operator's:

1. **Whether packets 17/23 (worktree + index) are pulled ahead of 11-i's contract.** The measurements
   say the fix is small; the *sequencing* is a plan change, and plan changes are not mine.
2. **The enforcement design's layer 3** (`docs/design/2026-07-25-package-first-enforcement.md` §5) —
   whether the module ledger earns its ongoing cost. Layers 1 and 2 I would build regardless.
3. **The findings' dispositions** — #206 (auth/session binding), #207 (five unjittered backoffs),
   #209 (watchdog private-internals fork). Each is real, none is 11-i's, and each needs an owner.

---

## 4. The process correction, recorded so it does not recur

The failure was not that the lead escalated too much in general — scope law is explicit that nothing
is deferred or dismissed without the operator, and that stands. **The failure was escalating
INSTANCES of a rule the operator had already given.** Once *"add any package that does the job"* is
stated, "should we adopt sklearn?" and "should we take the kubernetes dependency?" are not questions;
they are the rule being applied, and returning them for signature converts a decision into a queue.

**The test to apply before escalating, going forward:** *can this be derived from something the
operator has already said?* If yes, derive it, record the derivation, and move — the operator can veto
a recorded ruling far more cheaply than they can answer an open question. If no, it is a genuine fork
and it escalates, with a recommendation.

That distinction is what §1 and §2 of this document are: three genuine calls, twenty derivations.
