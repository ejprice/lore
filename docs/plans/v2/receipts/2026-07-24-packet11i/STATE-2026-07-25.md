# Packet 11-i — CURRENT STATE, 2026-07-25 (the one entry point)

Assembled by `lead-11i-b` at the end of the session that resumed 11-i after a system crash. **This
file exists because the state got spread across three documents and six findings during one session
— that was the lead's doing, and this consolidates it rather than handing the operator a
reconstruction job.** Every claim here is a pointer; nothing is re-derived, and where a number is
cited it names where it was measured.

**Reading order if you only read one thing:** §3 (what awaits you). Everything else is context for it.

---

## 1. SETTLED — nothing here needs your attention again

### 1.1 Your four rulings (2026-07-25), recorded at `CONTRACT-FREEZE-DECISIONS.md` §0
| ref | ruling |
|---|---|
| §A | Split at the store/runner seam. **Hold condition RESOLVED** — it was contingent on S1, which landed NOT DEGENERATE. See §4 for a re-sizing you should see. |
| §B | Ephemeral-container R2 vehicle against `:18500`, with three riders. **Riders since amended** — see decision 12. |
| §C | Adversary attacks C1–C14 before you rule. **Executed** — verdict CONTRACT INSUFFICIENT, 13/14, all adjudicated. |
| §D.12 | 10-d stays undeployed; cost accepted with a named re-open trigger. |

### 1.2 S1 — measured, and the packet does NOT fork
`REPORT-probe-bootstrap-11i.md` @ `f66b980`. **NOT DEGENERATE**, with the mechanism derived rather
than asserted: a nearest-rank interval collapses only if one value holds ≥90% of resampled mass; the
floor's value is one sample's own cosine, absent from a size-N resample with probability
`(1−1/N)^N → 1/e`, capping modal mass at ≈63.5% at N=56. Measured 25.8–64.8% across 25 runs.
**S1's second prediction also refuted:** D3 adopts in 0–1.5% on an unmoved floor while the point
floor moves 100% of the time.

### 1.3 The client consult — 3 blind informants, `CLIENT-CONSULT-SYNTHESIS.md` @ `e9b570f`
Unanimous and independent: show the near-misses always · the **margin pair** is the only wanted
number (all three invented the same render) · **refuse the CI** (same argument, unprompted: serving
it delegates the tool's decision to a party with less information) · deliver inline or not at all ·
**silence beats a hedged verdict** · trust loss is **categorical, not proportional**.

### 1.4 Measured environment facts — `CONTRACT-FREEZE-DECISIONS.md` §0.5
**M1** #134 is a MOUNT-TOPOLOGY problem, not a git-version problem — parent mounted at its identical
absolute path + uid mapping ⇒ container git 2.47.3 resolves both trees. **M2** `dubious ownership`
is the fourth silent cause behind `capture_git_identity`'s `(None, None)`, and under k8s a uid
mismatch is the DEFAULT. **M3** lore can create and read its own worktrees in-container; the
absolute gitdir stops mattering once lore owns both sides of the path. **M4** numpy+scipy install in
~1s.

### 1.5 The library equality proof — F-r2 §R9, commit `2b8d3fa`
**`sklearn.metrics.roc_curve` is proven exactly equal to `choose_cosine_floor`** — 20/20 randomized
trials matching floor, false-fire AND catch, on fixtures with duplicate cosines, anchors on both
arms and dominance tie-breaks. The boundary mismatch the lead measured (96/96) resolves to **one
index of shift**, not arithmetic. Differently-broken control: the naive mapping fails 44/75, so the
agreement is not vacuous.

**The transfer theorem, and it is why the correction was cheap:** because the library path is
*value-identical*, every probe measurement (§4b, both ladders, the flap tables) and every design
conclusion resting on them carries over **unchanged**.

---

## 2. VOID — superseded; do not rule these

| was | why it is void |
|---|---|
| **F9** (bootstrap affordability: sort-sweep, `--max-embeds` rationale, the 5.7h/rung crisis) | Scar tissue from a missing dependency. Replaced by **R3 + R9**. The crisis numbers must be **re-measured**, not carried. |
| **F10** (§B provenance riders: LORE_VERSION primary, `loremaster.__file__` demoted, optional gitdir mount) | Scar tissue from an unsupported worktree topology. Replaced by **R4/R8** + decision 12. |
| **Lease Option A** (epoch-fenced, clock-free reclamation) | Built to satisfy a prohibition that **was never ruled** (§0.4). Both clock-free legs are unsafe at N>1; leg (b) causes steady-state mutual theft between healthy pods. Replaced by **R2**. |
| **R2's hand-written election ALGORITHM** (incl. "store-clock authority") | Superseded by **R10** (`13a4dc2`): `kubernetes.leaderelection` is fully backend-agnostic — six-member lock interface, **zero kubernetes imports in the algorithm** — and its expiry test is *observer-relative* (`observed_time + lease_duration > now`, each pod on its OWN clock from when IT last saw a change). That is skew-immune **without any clock authority**, so store-clock authority answered a question the library never asks. ⚠ **R2's REQUIREMENTS analysis survives verbatim** and is what made the library evaluable in minutes — TTL, heartbeat, fencing-as-complement, cross-process single-flight, never-on-serving-path, the CAS discipline, and the N>1 analysis that killed both clock-free legs. |
| **C14** (ladder affordability) | Dissolved with F9. |
| **The relative-paths worktree route** | Retired from BOTH sides at once (§R8): unnecessary per M1/M3, and *hazardous* — `--relative-paths` silently bumps the main repo to format v1 + `extensions.relativeworktrees`, which git 2.47.3 must refuse **repo-wide**. |

---

## 3. AWAITING YOU — the decision set

Source of record: **F-r2 §R6** plus §R9's additions. Recommendations are the sidecar's unless marked.

### 3.1 Ratifications — stand as drafted, low-stakes, rule as a block if you like
1 (adopted-N rule + calibrated bound) · 3 (states + 30–49 join) · 4 (cause enum + two-degeneracy
split) · 5 (`head_identity` + `query_shape` reserved) · 6 (pins, including the explicit drop) ·
7 (eleven of the fourteen adjudications) · 9 (C11 manifest bound) · 10 (C13 bar re-registration).

### 3.2 The genuine forks
| # | fork | recommendation |
|---|---|---|
| **2** | **F3 — the ≥98% gate's sample population.** R-literal (answered union, what the ruled words say) vs R-package (adds the absent leg, anchored excluded). The two readings differ by **4.1 points at N=100 against a 2% budget** — repo law makes this a STOP, not a judgement call. | R-package, costs stated both ways |
| **8** | **C8 — a narrowed store projection inside a `DEPLOY: no` packet.** A new *unserved* read alters no served byte. | authorize as an 11-i-a addition |
| **12** | **The §B vehicle's provenance** — amends your own §0 ruling. Interim = path-identical mount + fail-loud-on-null; end-state = one-directory layout + real runtime git identity. ✅ *The stale "git ≥2.48" clause is now removed at source, with a visible correction marker rather than a silent edit.* | as drafted |
| **13** | **The lease — REPLACED BY R10.** `kubernetes.leaderelection` (the algorithm) + a **SurrealDB lock adapter** (~129 LOC, six members). Fencing is NOT in `LeaderElectionRecord` (four fields only) — it lives in the adapter: a `revision` counter playing resourceVersion's optimistic-concurrency role, plus `fence_epoch` bumped on holder change, both minted store-side in one UPDATE. CAS = `WHERE revision = $observed` through `retry_on_conflict`. `Config(60s/20s/5s)` declared. | library + adapter |
| **22** | **Take the `kubernetes` dependency** — measured at **21 packages for one 183-LOC pure module**. Vendoring rejected by name (it is hand-rolling with extra steps). ⚠ If we are targeting k8s anyway this is a rounding error rather than a cost — **operator's call, since it turns on deployment intent, not engineering.** | take it |
| **23** | **`release_if_held`** — an adapter-level extra, because **this port has NO `release`** (client-go does). Without it **every rolling update waits out the full `lease_duration`** before maintenance resumes. Found only by reading the installed source. | add it |
| **19** | **Adopt sklearn.** The numpy-`searchsorted` assembly exists as a two-package fallback but is parts-assembly of exactly the primitive #201 says not to rebuild. | adopt |
| **20** | **Which implementation is production.** With equality proven: roc-path as production + `choose_cosine_floor` as executable spec/**oracle** — versus keeping the O(N²) implementation on the hot path. The sidecar **self-revised its own containment pin** here rather than defending it. | the oracle |
| **21** | **BCa contingency.** Available and *not* recommended: adopting it would amend D8's pre-registered "central 90% percentile" — a ruling, not a default. | do not adopt by default |
| — | **Lease tunables** (60s, renew at duration÷3), **numpy/scipy/sklearn adoption record**, **layout inventory**, **the R5 extraction plan**. | as drafted |

---

## 4. THE PACKET'S SHAPE — one thing your §A ruling rested on has moved

The sidecar's answer to "does the split still describe the work", asked directly:

- **The LINE survives** — store/runner remains the right seam.
- **The 0.29 is superseded → ~0.24 ± 0.03.**
- **And b's character line was always slightly wrong:** 11-i-b was sold as "all the arithmetic," but
  its build cost was always *deliverables* — probes ~0.06, the R2 verb + C6 package ~0.06, the pin
  suite ~0.05. The arithmetic was the *runtime* crisis, and that is what collapsed. The build cost
  shrinks only modestly.

**Port sweep:** the arithmetic **third** of the survey's pure core dissolves (sweep, bootstrap, all
three percentile implementations, group-summary math); the domain **two-thirds** carries
(identity-pinned predicate/bars, probe derivation, hold-out logic, captures, sampler, reporters).
**The dominance suite becomes the ORACLE suite.**

**Sequencing (unchanged and still binding):** design addendum ruled → 11-i-a's contract → 11-i-b's
contract, with an explicit interface freeze in a that b cites.

---

## 5. FINDINGS FILED THIS SESSION

| # | subject |
|---|---|
| **#187** | `lore_remember`'s length notice advises a fix that cannot work; threshold never stated |
| **#197** | `capture_git_identity`'s silent `(None, None)` — #131's shape, still live, four causes |
| **#198** | `weighted_percentile` hand-rolled twice — **now deletable outright**, `np.quantile(weights=)` |
| **#199** | `scripts/` has no `testpaths` entry — third independent report asking |
| **#200** | Nothing distinguishes operator-RULED from design-author-ASSERTED after one restatement |
| **#201** | The statistical core is a hand-rolled reimplementation of sklearn/scipy/numpy primitives |
| **#202** | `_txn.retry_on_conflict` is the hand-roll pattern's third instance (CLAUDE.md's own tenacity example) — **deliberately NOT churned** (proven, guarded, eleven mutation-proven consumers, runtime guard keyed on its `__code__` frames), with a named re-open trigger: the next *behavioural* change to the retry policy evaluates tenacity FIRST |
| **#203** | `surrealdb-31-capabilities.md` §6.6 item 7 cites a "four-way-ambiguous CAS return (§5)" that §5 never enumerates — a dangling pointer in the REQUIRED-FIRST-READ store doc. Verified: `four-way` occurs **once** in the file, on the citing line itself |

---

## 6. OPEN RISKS — not decisions, but do not let them go quiet

1. **An undiagnosed 35-minute CPU wedge** in a pure-arithmetic loop during the probe's second run.
   Unreproduced. The code it occurred in is now slated for replacement by library calls, which
   removes the substrate **without ever explaining the cause** — worth one line in the R2 receipts
   rather than silent disappearance.
2. **`safe.directory` must become explicit image state** (M2). Under k8s, uid mismatch is the
   default, and this is one of the four causes that renders as a silent `(None, None)`.
3. **RWX-PVC git locking** if multiple pods share a worktree volume — bounded by R5's leader
   election, but named.
4. **`scipy.stats.bootstrap` exposes both `rng` and `random_state`** in 1.18.0 (transition dual).
   Pin one; the other is #198's drift class waiting to happen.
5. **The `paired=` fit** for our structured unit (paired two-leg probes + unpaired identifier arm)
   is the one remaining contract-author investigation — the sidecar read the signature, not the full
   paired semantics, and said so rather than asserting.
6. **Two inert scratch residues** — `/tmp/wt-probe`, `/tmp/roc_probe.py`. The sandbox denies `rm -rf`
   to both the sidecar and the lead. Uncited; substance reproduced in-file.

---

## 7. WHAT THIS SESSION ACTUALLY CHANGED

The packet arrived believing it needed to solve an affordability crisis, a degeneracy risk, and a
provenance problem. **All three were self-inflicted:** the crisis by a hand-rolled ROC sweep, the
degeneracy analysis by a hand-rolled bootstrap, the provenance problem by a mount topology. The
operator caught the first in one sentence; the second dissolved with the first; the third was one
`podman run` away from being measured rather than designed around.

The measurements survive all of it — that is what §1.5's transfer theorem buys.
