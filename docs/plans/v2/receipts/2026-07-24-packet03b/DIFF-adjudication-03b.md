brief-base v6 read

# DIFF-adjudication-03b — blind rulings r2 vs the struck record

**Adjudicator:** design-comms-03b-r2 (diff-adjudicator role; holds the struck corpus in
context by design — the blind designer must never see this document's struck-side inputs).
**Adjudicated 2026-07-24 at HEAD `29708f5`** (blind doc committed; test tree = the `0941aab`
reset baseline). **Inputs:** `docs/plans/v2/03b-design-rulings-r2.md` + `REPORT-design-blind-03b.md`
(read fresh) · the struck S1–S8 rulings + struck corpus (tag `pkt03b-tainted-corpus`) + both
struck adversary reports (held in context) · the 34-item hazard checklist
(REPORT-design-comms-03b-r2.md, post-supersession addendum — the completeness grader).
**Binding rule, honored throughout:** every amendment candidate carries a derivation from
TRUSTED sources or a FRESH probe run this session; the struck material points at questions
only — no verdict below cites it as justification. Probe receipts: §P at the end.
⚠ One self-correction, per the re-derive law: my earlier message called the checklist
"27 items"; the derived count is **34** (9 clusters). The wrong number was mine.

Classes: **CONVERGENT** (blind independently derives what the struck record concluded —
corroboration, the strongest state) · **BLIND-SILENT** (struck record knew it; blind misses
it → amendment candidate, AC-nn) · **CONTRADICTION** (blind rules X, record concluded Y —
adjudicated) · **NOISE** (no surviving relevance) · **BLIND-NET-NEW** (blind derived a door
the whole struck apparatus never probed — credit).

---

## §1 VERDICT TABLE (checklist items A1–I1, then diff items X1–X10)

| item | subject | verdict | disposition |
|---|---|---|---|
| A1 | D1 weak fleet marker rewards wrong drain arithmetic | **BLIND-SILENT** | AC-01 → FK-2 (probe ✓ §P1) |
| A2 | ACK-REQUIRED `acked_at` monoculture | **CONVERGENT** | blind B4.4 mandates the R6 discriminating fixture |
| A3 | `"{msg}"`-only false-gate pin | **BLIND-SILENT** | AC-02 (constraint has no instrument; probe ✓ §P2) |
| A4 | reach-pin omits the three new helpers | **BLIND-SILENT** | AC-03 (pin is a FLOOR, probed ✓ §P3 — silent gap real) |
| A5 | 2 ledger mypy errors unpayable by build | **BLIND-SILENT** | AC-04 → FK-4 (probe ✓ §P4) |
| B1 | CONTENT⊕SET/MERGE; `object::extend` | **CONVERGENT** | blind's `LET`+inline-CONTENT shape derives from trusted `messages.py::_send_fragment`; store-ref carries the fact at HEAD (✓ §P5) |
| B2 | `SELECT *` omits NONE columns | **BLIND-SILENT** | AC-05 (store-ref §2 clause present ✓ §P5; adversary re-probes on contact per lead ruling) |
| B3 | `nextval` starts at 0 | **BLIND-SILENT** (minor) | folded into AC-05 |
| B4 | `:18500` count=0 single-sourced | **NOISE** | defused — blind's all-`option<>` schema makes the premise non-load-bearing; re-derivation stays the lead's standing item |
| B5 | `7f23223` store-ref status | **NOISE** | lead-ruled (re-probe-on-contact); facts present at HEAD ✓ §P5 |
| C1 | one ServerSession per MCP session | **NOISE** | moot — blind keys nothing on the session OBJECT |
| C2 | no middleware API; two-line `call_tool` | **CONVERGENT** | blind re-probed with positive control (its §Probes 1–2) |
| C3 | cancellation: finally-write survives; except-arm loses the row | **CONTRADICTION** | blind T5.3 accepts a loss its own T5 shape doesn't incur → AC-06 (fresh probe ✓ §P6, 3 legs) |
| D1 | `id()` recycling defeats coexistence-quantified keys | **NOISE** | moot — no minted key; correlator is SDK `uuid4().hex` (✓ §P7, cannot recycle) |
| D2 | uniqueness⊕retention interlock | **NOISE** | moot — no server-side key map exists in the blind design |
| E1 | no-op fix: renders built, never called | **BLIND-SILENT** | **AC-07 (highest-priority candidate)** |
| E2 | instructions substring gaps (cap value; invertible semantics) | **BLIND-SILENT** (partial) | AC-08 |
| E3 | ACK-REQUIRED taught command tail (`seqs=[]`) | **BLIND-SILENT** | AC-09 (marker stops at `"ACK REQUIRED: #71"` ✓ §P1) |
| E4 | grade-blind trailer LISTING | **BLIND-SILENT** | AC-10 (committed proofs discriminate grade-GATING only ✓ §P1) |
| E5 | session-omitted broadcast leak | **CONVERGENT** | blind B2.2 derived the door AND named the committed monoculture |
| E6 | S1 coverage-premise probe result | **CONVERGENT** | blind B8.6 demands live re-execution (correct posture) |
| F1 | served≡limit alignment on `hit_count` | **NOISE** | moot — blind design has no `hit_count` writer; note-for-future recorded in AC-12 detail |
| F2 | broadcast=False monoculture; hardcoded `thread` | **BLIND-SILENT** (partial) | AC-11 (`_p03_entry` re-defaults `acked_at`, hardcodes `thread` ✓ §P1) |
| G1 | D3-class double false gate (`_has_literal_text`) | **BLIND-SILENT** | folded into AC-02 (`" "` control confirmed ✓ §P2) |
| G2 | index-pin FIELDS-split trap · (extended) no 06-read index | **BLIND-SILENT** | AC-12 — blind ships NO trace index; the free window closes at deploy (§1.5 derivation) |
| G3 | vacuous empty-collection passes | **BLIND-SILENT** | AC-13 |
| G4 | canonicaliser denies the obvious seqs-join shape | **BLIND-SILENT** | AC-14 |
| G5 | harness double hides missing production wiring | **BLIND-SILENT** (partial) | AC-15 (`drain_limit` contract-forced ✓ §P8; `message_ledger` wiring unpinned) |
| G6 | seq-typing collateral-RED incentive | **CONTRADICTION** | blind §0.F6 "amendment-free" is FALSE — 2 committed pins go RED under T2/T8 (✓ §P9) → AC-16 → FK-3 |
| H1 | telemetry [real]-legs need the drain verb | **BLIND-SILENT** (minor) | AC-17 (the KEY rule enables synthetic-tool decoupling) |
| H2 | mypy-zero unpayable by either wave alone | **BLIND-SILENT** | folded into AC-04 |
| H3 | harness derived-count pin fires cross-suite | **BLIND-SILENT** | AC-18 (pin confirmed ✓ §P10) |
| H4 | merged all-suite satisfiability gate | **NOISE** | already adopted by the lead; binds the clean phase |
| I1 | production prose corpses | **CONVERGENT** (partial) | blind T5.2 ruled against the fire-and-forget corpse; full sweep → AC-19 (fresh enumeration ✓ §P11) |
| X1 | **blind render vocabulary vs the COMMITTED registry** | **CONTRADICTION (headline)** | AC-20 ruling + FK-1 (✓ §P1 — the committed templates FORCE their wording) |
| X4 | peek-trailer rule unstated | **BLIND-SILENT** | AC-21 |
| X5 | drain row renders `thread` unconditionally (contradicts committed `{context}` cell AND blind's own B3.2 principle) | **CONTRADICTION** | AC-22 |
| X6 | elision arithmetic returns with the committed `{next_limit}` slot | **BLIND-SILENT** | AC-23 |
| X9 | consumer-battery gaps (peek · broadcast · unknown-seq) | **BLIND-SILENT** (partial) | AC-24 |
| X14 | blind B10 row 1: "the R2 pin is still owed" | **CONTRADICTION** | AC-25 — both pins EXIST at baseline (✓ §P12); trusted 03a-2 doc says do-not-re-author |
| X7 | `set_status` closed vocabulary (typo = silent false-NOT-waiting) | **BLIND-NET-NEW** | credit — a door no struck pass probed |
| X8 | retired-recipient reject (undrainable-forever delivery) | **BLIND-NET-NEW** | credit — same |
| X10 | struck telemetry machinery (annotation channel, closed key set, WB1/MP4, uuid key mint, WeakKeyDict, MP3 pins, rung-1 fact, hit_count enrichment) | **NOISE** (cluster) | all moot under the blind architecture — enumerated §3.4 so nothing is silently dropped |
| X12 | `ok` column vs "an errored pull is still a pull" | **NOISE** | the column records both readings; packet 06 chooses its numerator filter — strictly more informative |

**Headline counts (42 adjudicated rows + 2 credits):**
**CONVERGENT 6 · BLIND-SILENT 22 · CONTRADICTION 5 · NOISE 9 · BLIND-NET-NEW 2.**
**Amendment candidates: 25 (AC-01…AC-25), of which 15 carry fresh probe/grep receipts**
(the rest derive from trusted law/docs, as the binding rule permits).

---

## §2 The headline contradiction — X1, and the AC-20 ruling

**Measured (✓ §P1):** the committed, IMMUTABLE promise registry at the reset baseline
already fixes the render vocabulary as classified/registered template literals:
`drained {shown} of {total} pending` · the peeked header (with its `re-run without
peek=true` teach) · the row `#{seq} [{grade}] {sender}→you{context}: {body}` (+refs
variant) · `+{more} more unread — re-run with limit={next_limit}` · `no unread messages` ·
`sent #{seq} [{grade}] → {recipients}` (+cap variant) · `sent … → broadcast: {count}
agents in session {session}` · `already acked: {seqs} — no new stamp` ·
`not addressed to you: {seqs} — …` · `ACK REQUIRED: {seqs} — lore_comms action=ack
seqs=[{seqs_csv}]`. The registry's own committed force
(`test_registered_iff_proven` / `test_no_dead_registry_entries`) requires every registered
literal to exist in `server.py` — **the committed vocabulary is not advisory; it is the
render's obligatory wording.** The blind doc designed a fresh vocabulary (`inbox: {total}
unread…`, `drain again for the rest`, `inbox empty — 0 unread`, per-entry ack lines,
`#{seq} … · thread {thread} · sent {age} ago` rows, fenced bodies) that contradicts it in
at least eight places. This is the structural cost of blindness: the blind designer read
the registry "targeted" and its report claims *"NO send/drain/ack render pins exist"* —
true of behavioural shape pins, false of the template literals.

**AC-20 (RULED — compelled by the transcribed operator immutability ruling, not a
preference):** the fresh contract builds on the committed vocabulary wherever a committed
template exists; the blind doc's B3–B5 contribute at the SEMANTIC layer (emit predicates,
keying, scoping, ordering, honesty properties), grafted onto committed wording. Per-template
disposition:

| blind template | committed state | disposition |
|---|---|---|
| B4.1 header `inbox: {total} unread ({directives} directive)…` | `drained/peeked {shown} of {total} pending` committed | committed wins; the directive count is the 04 counts-family (trusted packet Scope OUT) — dropped from 03b |
| B4.2 row + `· sent {age} ago` + fenced body | row template committed, body INLINE | committed wins on shape; the age cell is dropped (§1.1 attention cost); **the body slot is FK-1** |
| B4.2 `· acked {age} ago` re-serve marker | no committed acked-visibility template | the R6 clause-3 need is real; land as an ADDITIVE new template (trailer or row suffix — contract author's choice, registered + proven) |
| B4.3 elision `drain again for the rest` | `+{more} more unread — re-run with limit={next_limit}` committed | committed wins → the `{next_limit}` arithmetic question returns → AC-23 |
| B4.1 empty `inbox empty — 0 unread` | `no unread messages` committed | committed wins |
| B3.1 broadcast receipt `→ {n} agents (broadcast, session {s})` | `→ broadcast: {count} agents in session {session}` committed | committed wins |
| B5 ack per-entry lines | four ack templates committed | committed wins; blind's derived-prose mechanism (ONE dict keyed by `AckOutcome`, set-equality pinned) is KEPT — it is a semantic-layer ruling and grafts cleanly |
| B3.3 question teach · B4.1 peek-suffix · skew-at-drain reuse | no committed counterparts | blind wording free (new registrations per B8) |

**FK-1 (operator fork, the one genuine trusted-source conflict):** the packet's own exit
(trusted at `0223291`, quoted by the blind doc itself) demands *"a hostile body staying
inside its fence"* — while the committed row template renders `{body}` INLINE. Both cannot
hold. Recommendation: **fence** — bodies are the one surface where another agent's free
text lands inside a consumer's context; inline sanitisation flattens newlines and makes
multi-line bodies unreadable; the blind doc's B4/B7 rationale is independently right.
Cost: one operator-authorized amendment wave over the two committed row-template entries
(+ their proofs), mutation-proven. The alternative (read "fence" loosely as
sanitised-inline) weakens the exit's plain meaning and should be chosen only if the
operator prefers zero committed-registry edits.

---

## §3 Per-item detail (candidates AC-01…AC-25; each with its derivation)

**AC-01 [→FK-2] The fleet elision marker must be promoted to a full line.** Fresh
derivation (✓ §P1, no struck citation needed): the committed fleet proof's marker is
`f"{_EM_DASH} re-run with limit=5"` — value-bearing but line-incomplete — and the committed
drain elision proof fixture (2 shown / 7 pending / limit 2) renders, under the honest
remainder arithmetic (AC-23), `+5 more unread — re-run with limit=5`, which CONTAINS the
fleet marker verbatim. The cross-satisfaction meta-test then fires — against the CORRECT
build — the moment `_render_comms_drain` exists; under the dishonest `shown+more`
arithmetic it renders `limit=7` and stays green. A marker that is a strict substring of
another template's plausible instantiation cannot discriminate; concealment by fixture
re-numbering leaves the weak marker armed. Fix: one-line, strengthen-only marker promotion
(`+3 more — re-run with limit=5`) — operator authorization (FK-2). #182.1's owner.

**AC-02 The placeholder-only ban needs an INSTRUMENT, not a constraint.** Blind B8.4
states the ban as prose ("a builder who finds they need `render_line(\"{lines}\")` has hit
the trigger — STOP"). The committed bound pin asserts only `"{msg}" not in _classified()`;
a builder classifying any OTHER placeholder-only template passes it. Candidate: an
ADDITIVE ∀-pin over `_classified()` banning placeholder-only templates, in the
`_is_placeholder_only` form (≥1 format field AND nothing but whitespace outside the
fields), with the `" "` join separator as its pinned positive control — `" "` is confirmed
in `_PROMISE_FREE` (✓ §P2), and any "has literal text" formulation both rejects `" "`
(whitespace-only, no field, nothing to smuggle) and ACCEPTS `"{msg}"` (brace characters
read as text). Derivation: the committed bound's own docstring class + the
prose-vs-assertion false-gate law (repo CLAUDE.md). #182.3's owner.

**AC-03 The reach pin is a FLOOR; the three new helpers must be demanded additively.**
Probed (✓ §P3): `missing = expected - observed_functions; assert not missing` — new
helpers outside `expected` are silently un-demanded. Candidate: an ADDITIVE pin in the
fresh contract asserting `{_render_comms_send, _render_comms_drain, _render_comms_ack} ⊆
observed_functions` (no committed edit needed). #182.4's owner.

**AC-04 [→FK-4] The two ledger mypy errors need an authorized closed-contract edit; and
global mypy-zero is a BOTH-BUILDS property.** Probed (✓ §P4): `_ask(ledger: Any, …) -> int`
(and `_answer`) return through an `Any` — the classic no-any-return pair; the lead's reset
receipt fixes the total at 36. No build pays these two; the fix is a `cast(int, …)` — a
closed-packet contract edit (the `42eeedc` precedent) → operator fork. The exit
measurement must be taken AFTER both waves land, never attributed to the surface builder
alone.

**AC-05 Explicit projections when reading `option<>` trace columns; ordinals are 0-based.**
The store reference at HEAD carries both facts (§2's `SELECT *` OMITS-a-NONE-column clause
and the `object::extend` idiom — ✓ §P5), tagged with their probe provenance; the lead has
ruled the fresh adversary re-probes on contact. Candidate: the telemetry contract's read
legs use explicit projections (a `SELECT *` read of an unset `option<>` column KeyErrors,
which reports as harness failure, not finding), and no pin may assume 1-based ordinals.

**AC-06 Pin the cancellation leg — the blind design already earns it.** Fresh probe
(✓ §P6, three legs): an awaited write in `finally` COMPLETES when the task is cancelled
(`['finally-arm']`); the write placed in the `except Exception` arm + success path loses
the row on cancellation (`[]`); positive control (success path) writes exactly once. Blind
T5's own pseudocode puts the write in `finally` — so its T5.3 concession ("a cancelled
dispatch may lose its row — accepted") under-claims its own mechanism and, worse, leaves
the door open for a builder to "simplify" `finally` into except+return arms, silently
un-tracing exactly the cancelled/timed-out population (a struggling session's calls — the
denominator packet 06 most needs). Candidate: one pin — a cancelled dispatch still writes
its row — plus the T5.3 prose corrected to match the mechanism.

**AC-07 [HIGHEST] Per-verb dispatcher-serves-render pins.** Derivation: the repo's own #94
law (trusted CLAUDE.md): *"every pin tested a new method nothing required the code to
CALL."* The blind doc pins render SHAPES (B3–B5) and dispatcher SEMANTICS (B2), but
nothing it demands fails when a handler never calls its render — a handler returning any
fixed string satisfies every shape pin (they drive the render directly) and every ledger
pin (they drive the ledger directly). Candidate: for EACH of send/drain/ack, one pin that
drives the REAL dispatcher and asserts the verb's render output (receipt line, row+trailer,
ack group lines) reaches the returned text. The blind self-attack's D-series covers every
door EXCEPT this one — it is the single most valuable pin the fresh contract can add.

**AC-08 Teaching values derive from constants; semantic claims pinned non-invertibly.**
Blind B9 makes each clause a substring pin — necessary, insufficient: a substring pin on
"bodies ≤ 2000 chars" passes if the constant changes (prose-beside-behaviour, the #104
law); peek/grade clauses pinned as loose substrings admit inverted rewrites. Candidate:
the body-cap pin extracts the served integer and compares to `MESSAGE_BODY_MAX_CHARS`;
the peek/grade/broadcast clauses are pinned as ordered, non-invertible assertions.

**AC-09 The taught ack command must be RUNNABLE.** The committed ACK-REQUIRED proof
marker is `"ACK REQUIRED: #71"` (✓ §P1) — it stops before the `seqs=[…]` tail, so a build
serving `seqs=[]` passes the proof while teaching a command that discharges nothing.
Candidate: a pin splitting the trailer at `action=ack ` and asserting the taught list
names every demanded seq (the registry's own §9.7 litmus: if the reader ran the taught
command with its defaults, would the promised thing happen?).

**AC-10 The mixed-grade LISTING fixture.** Probed (✓ §P1): the committed proof pair varies
grade for the EMIT decision (directive emits, lone signal doesn't) — so grade-GATING is
discriminated, but a build that gates on directives and then LISTS every unacked entry
(signal included) passes both legs. Candidate: one window holding an unacked signal AND an
unacked directive → the demand names the directive only. Derivation: a two-conjunct emit
predicate needs a discriminating fixture per conjunct AND per role (gate vs list) —
fixture-discrimination law.

**AC-11 Fixture-default law applied to the fresh drivers.** Probed (✓ §P1):
`_p03_entry(*, seq, grade="signal", acked_at: Any = None)` — `acked_at` defaulted again
and `thread` hardcoded with no parameter, on the factory feeding proofs whose predicates
branch on both (B4.4 keys on `acked_at`; the row's `{context}` cell keys on thread-vs-
session per AC-22). Candidate: the fresh contract's drivers make both REQUIRED at every
call site (repo law verbatim: no default on a branched-on parameter), and the send-render
battery gains the `thread`-field RenderCase the moment B3.2/B3.3 put `thread` into the
send render (per-FIELD, since battery completeness is per-ACTION). Covers #182.5's
injection half; also carries the broadcast-render fixtures (a `broadcast=True` +
`question=True` leg — if the render can branch on it, one pin must differ).

**AC-12 Ship the 06-read trace index inside the free window — or decide not to, on the
record.** The blind schema ships columns and NO index. Derivation (trusted store reference
§1.5 + the 03a-2 R3 window argument pattern): a new index on a populated table BUILDS,
blocking, at the first `ensure_ready` carrying it; the trace table is empty until 03b
deploys and grows every call thereafter — so an index added by packet 06 builds over weeks
of all-tools rows at every store's next boot, while the same line shipped with 03b is
free, once, forever. Candidate: rule the 06-read index now (shape suggestion:
`(agent, ordinal)` — 06's per-agent curve; `transport_session` reads can ride a second
plain index only if 06's design demands it), as its own one-concern commit before deploy.
Rider: any schema pin asserting index fields must parse the `FIELDS` clause, never
substring the whole statement (an index NAMED `trace_agent_ordinal` contains both
substrings). Note-for-future (F1's residue): if any wave later adds a served-count writer,
the `limit>pending` twin fixture is mandatory at that moment.

**AC-13 Non-vacuity guards on every ∀-over-collection pin.** `all()`/`sorted()`/
`len(set())` are trivially true of empty collections; "the call succeeded while the store
was broken" is trivially true of a build with no telemetry. Candidate: contract-phase
discipline line — every such pin leads with an explicit non-emptiness guard whose failure
message says so. Derivation: a probe that cannot fail is worth nothing (contract-adversary
role spec P0, trusted).

**AC-14 The trailer-seqs assembly will hit the canonicaliser's deny-by-default.** The
committed canonicaliser treats comprehensions/generators as opaque-or-denied shapes
(✓ §P3's neighbouring read) and the committed `{seqs_csv}` slot must be fed through the
sanctioned render machinery. Candidate: the contract author derives the working assembly
shape against the committed deny pin BEFORE fixing the drivers, and records it in the
driver's docstring — one wall-hit bought once, on purpose.

**AC-15 Production-wiring pins (the harness-double blind spot).** `drain_limit` is already
contract-forced (✓ §P8). NOT forced anywhere: `AppContext` accepting/constructing the
`MessageLedger` the handlers need. A `SimpleNamespace` harness double declares the
dependency into existence; the suite then passes with production unable to construct the
surface (the test-environment-is-a-fiction law, trusted). Candidate: introspection pins —
`AppContext.__init__` accepts `message_ledger`; `build_app_context` constructs one — with
a positive control over an already-wired sibling (`brief_ledger`) so the introspection
demonstrably sees wiring.

**AC-16 [→FK-3] The blind §0.F6 "amendment-free" claim is FALSE — two committed pins go
RED under the T-series.** Probed (✓ §P9): `test_surreal_schema.py::…test_trace_core_
scalar_fields_are_defined` asserts `"TYPE int"` for `hit_count` and `"TYPE string"` for
`session` — blind T2 loosens both to `option<>` → RED; `test_surreal_fakes.py::
test_record_trace_signature_matches_real_store` asserts the EXACT 7-name parameter list —
blind T8 adds four params → RED. (The blind read `test_surreal_store.py` only; the claim
died in two files outside its source list.) Candidate: two operator-authorized amendments
(field-def pin gains the loosened types + new columns; signature pin gains the new list),
strengthen-only, mutation-proven — batched as FK-3. Everything else in T2 confirms
amendment-free: the raw-CREATE round-trip pins and the flagship migration pin stay green
because every new column is `option<>` (the all-option choice also kills the wrong-schema
incentive: there is no required column whose removal greens committed pins — the blind
design is CHEAPER here than any required-column alternative, adjudicated in its favor).

**AC-17 Decouple the telemetry identity legs from the comms build.** The blind KEY rule
(harvest `agent`/`session`/`action` from any tool's declared arguments) means the identity
pins can drive a SYNTHETIC probe tool declaring those params — no dependency on the comms
verbs landing first. Candidate: one-line contract note; kills the cross-wave 2-RED tail
class.

**AC-18 The harness derived-count pin will fire on the first new test file — expected
collateral, named in the brief.** Pin confirmed (✓ §P10; docstring counts 35/21 at
baseline). The fresh telemetry contract file will import the harness → importer count
moves → pin RED; the SECOND derived count (`connect_admin` callers) hides behind the first
assert and must be re-derived in the same edit. Candidate: the contract-wave brief carries
the docstring bump as an expected, authorized, re-derived-not-hardcoded collateral.

**AC-19 The prose-corpse retirement sweep rides the builder brief.** Fresh enumeration
(✓ §P11): `store/surreal.py::record_trace` docstring teaches "fire-and-forget EMISSION …
a later serving-layer phase (P8d)" and "six core fields"; `server.py` `TraceSummary` area
says "no caller yet wires the per-invocation record_trace emission"; `surreal_schema.py`
carries "six core + two accounting columns" at three sites. All become false (one becomes
the REFUSED posture) the moment the builder lands T1–T8. Blind T5.2 already rules against
the fire-and-forget plan; the candidate adds: the builder brief lists these sites for
retirement, and the sweep greps are bare and anchor-free (rename-sweep law).

**AC-21 The peek-trailer rule must be settled, and the trusted derivation says
stamping-only.** Blind B4 gives peek variants for header and elision but leaves the
ACK-REQUIRED trailer's peek behaviour unstated — a builder decision by default. Derivation
(trusted 03a-2 R6): the anomaly path R6 accepts is peek→ack; a trailer DEMANDING acks on a
peek manufactures that path at scale, and the committed peek header's own teach ("re-run
without peek=true to mark them seen") frames peek as look-only. Candidate ruling: trailers
render on STAMPING drains only; pin = a peek over an unacked directive contains no
`ACK REQUIRED`.

**AC-22 The drain row's thread cell: blind contradicts the committed template AND
itself.** The committed row template carries ONE `{context}` cell; blind B4.2 renders
`· thread {thread}` unconditionally on every row while blind B3.2 rules the send render
suppresses the session-default thread because *"the default thread IS the session;
echoing it … is per-token noise with zero signal."* That principle applies identically to
drain rows — most traffic rides the session-default thread, and an unconditional label
destroys the "a thread label means a deliberate conversation" signal blind B9.5 leans on.
Candidate ruling: the `{context}` cell is singular with precedence task-then-thread, the
thread half renders only when `thread != session`, the render signature therefore carries
`session` REQUIRED (the comparand must reach the render; a defaulted comparand lets any
call site silently kill the branch — fixture-default law at the signature layer), and the
pins include the discriminating pair (same render: default-thread row bare,
deliberate-thread row labelled) plus a literal-vs-argument leg (a driver passing a
non-`wave7` session inverts which row draws the cell).

**AC-23 The elision arithmetic pin: `next_limit == total_pending − shown`.** With the
committed template's `{next_limit}` slot back in force (AC-20), the value must be ruled or
every builder guesses. Derivation from trusted production semantics (`messages.py::drain`
stamps exactly the served window; stamped rows never re-serve — the no-cursor law): the
honest re-ask is the REMAINDER; `shown + more` re-asks for rows that cannot re-serve.
Pin the arithmetic on the committed fixture (5 vs 7 vs 2 are pairwise distinct there —
✓ §P1) with a docstring naming the fleet contrast (fleet re-serves; drain does not).

**AC-24 Consumer-battery additions.** Blind C2's eight tasks miss three ruled teaching
surfaces: peek consequence (what you peeked is still unread — the R6 tripwire B9 itself
calls the only user-facing one), the broadcast form (`to=[]`), and the unknown-seq
recovery (C2 task 8 covers `not_addressed` only). Candidate: three added tasks, same
machine-checkable key discipline. Gate shape recommendation (folds the adopted RC4
candidate input with blind C3): floor-model (Sonnet) 3-consecutive gates every render
change — blind's design, kept; at PACKET EXIT the full population runs once (one Sonnet,
one Opus, one Fable) and ANY keyed failure is an adjudicated finding, not a waived
receipt — the operator named all three as the client, so "the floor passed" cannot clear
a failure by a named client member. (Blind's capability-monotonicity assumption is
thereby demoted from load-bearing to convenience.)

**AC-25 Strike blind B10 row 1's "the pin is still owed".** Probed (✓ §P12): both R2
self-addressed pins exist at the reset baseline (`test_an_explicitly_SELF_ADDRESSED_
message_on_the_question_thread_is_NOT_an_answer`, `test_a_self_addressed_message_with_
ANOTHER_recipient_is_STILL_not_an_answer`) — shipped into `test_message_ledger.py` during
03a-2 (trusted), and the trusted 03a-2 doc's R2 item 3 says verbatim "do NOT re-author it
in 03b". Re-authoring would duplicate committed pins and invite drift. One-line correction
to the blind doc's disposition table (routed to the blind designer via the lead).

### §3.4 The moot cluster (X10), enumerated so nothing is silently dropped

Under the blind architecture (declared-params harvest + SDK-minted transport correlator;
no annotation channel; no server-minted caller key), the following struck-record machinery
has NO counterpart and its hazards no purchase: the ContextVar annotation channel and its
closed key set, raise-placement question, and leak pins; the drain-only-annotator and
register/heartbeat-annotation obligations (every `lore_comms` action carries `agent=`/
`session=` as declared params, harvested uniformly by the KEY rule — receiver-blind, no
per-action wiring to forget); the uuid caller mint, `WeakKeyDictionary`, recycling and
retention pins; the ServerSession rung fact; `hit_count`/`pending`/`peeked` drain
enrichment (no writer — honest NONE). My prior ruling that register/heartbeat annotation
is "required in scope" (report addendum, FLAG rulings context) is VOID with the rest of
the struck-layer retentions — the blind design satisfies the underlying need (identity on
every comms row) by construction. One residual carried forward: rows with `agent` set but
`session` and `transport_session` both NONE pool same-named agents across sessions;
visible in the data, bounded, for 06's analysis notes.

---

## §4 Blind self-attack D1–D15, graded (task a)

**Strong — attacks that hit doors the record knows are real:** D1 (session-omitted
broadcast scope — the cross-session leak class, with the committed monoculture named);
D6 (window-scoped trailer's small-N door, elided-directive fixture demanded); D7 (header
counts `total = len(entries)` — the lying-header class); D8 (ack re-order/dedupe — the
request-order class); D9 (seam bypass, with the honest named bound); D11 (identity: the
four attacks that matter for ITS design — and the two the record would add, recycling and
retention, are structurally moot: correlator = SDK `uuid4().hex`, no server-side map;
✓ §P7); D14 (eval gaming, both directions).

**Cluster verdicts the lead asked for:** cluster D (identity) — PASS, by architecture
plus D11; cluster F (fixture-arithmetic) — PARTIAL: D6/D7 cover window and header small-N;
missed the mixed-grade LISTING fixture (AC-10) and the fixture-default law on the thread
comparand (AC-11); served≡limit is moot (no writer).

**Missed doors, all now candidates:** the no-op fix per verb (AC-07 — the largest); the
committed-vocabulary force (X1/AC-20 — the largest blind spot, a structural blindness
cost); the peek-trailer rule (AC-21); its own §0.F6 false "amendment-free" claim (AC-16 —
two files outside its read set); the B10-row-1 misread of a trusted doc (AC-25); the
internal send-vs-drain thread-cell inconsistency (AC-22).

**Grade: GOOD on the doors of its own invented design; WEAK on committed-corpus
interaction doors.** That split is exactly what blindness buys and costs — and the diff
step exists to pay the cost, which this document does.

---

## §5 Finding #182 — the five baseline blockers, each with a named owner (task c)

| # | blocker | addressed by | owner |
|---|---|---|---|
| 1 | D1 weak fleet marker | NOT addressed by the blind doc; AC-01/FK-2 (marker promotion) + AC-23 (arithmetic pin) | operator (FK-2) + contract author |
| 2 | ACK-REQUIRED `acked_at` monoculture | blind B4.4's mandated R6 discriminating fixture (additive; committed proofs stand, superseded in force) | contract author; adversary P2 confirms |
| 3 | `"{msg}"` under-enforcing pin | blind B8.4 constraint + AC-02's additive ∀-instrument | contract author |
| 4 | reach-set gap | AC-03 additive subset pin (floor shape confirmed) | contract author |
| 5 | `send.thread` injection case + 2 ledger mypy | AC-11 (per-field RenderCase, forced by B3.2/3.3 putting `thread` in the send render) + AC-04/FK-4 (mypy casts) | contract author + operator (FK-4) |

No blocker is left ownerless; the builder-brief gate ("no builder briefed while any is
unadjudicated") is dischargeable through FK-2/FK-4 plus four contract-phase items.

---

## §6 Consolidated fork list, ranked (task b — one batch for the operator)

1. **FK-1 — fenced bodies vs the committed inline row template** (trusted-source
   conflict: packet exit vs registry literal). Recommend: FENCE; authorize the two
   row-template registry amendments + proofs. [§2]
2. **FK-2 — fleet elision marker promoted to a full line** (one-line, strengthen-only,
   mutation-proven; kills #182.1's class at the root). Recommend: authorize. [AC-01]
3. **FK-3 — two collateral committed amendments for the telemetry schema/signature**
   (field-def types; fake `record_trace` list). Strengthen-only, mutation-proven,
   batched. Recommend: authorize. [AC-16]
4. **FK-4 — the two `test_message_ledger.py` casts** (closed-packet edit, `42eeedc`
   precedent). Recommend: authorize. [AC-04]
5. **FK-5 — consumer-eval spend + gate shape** (blind E1 + AC-24: floor-model cadence
   gate; full three-model population at packet exit, any keyed failure adjudicated).
   Recommend: approve. [blind E1]
6. **FK-6 — skew-at-drain** (blind E3). Recommend: as blind ruled (shared helper,
   mutation-proven), with one pre-flight: verify the skew literals' registry
   descriptions/predicates are action-agnostic before reuse; if any names "heartbeat",
   its description edit rides FK-3's batch.
7. **FK-7 — the DESIGN-LAW §14 telemetry carve-out** (blind E5). Recommend: confirm
   blind's reading; AC-06's cancellation pin and the loud-log pin are the carve-out's
   own instruments.
8. **FK-8 — stale packet label in the committed contract** (blind E2). Recommend:
   bundle with the contract wave, as blind proposed.
9. **FK-9 — consent batch, no controversy:** blind E4 (constants as ruled) · E6
   (`fastmcp` migration NO, ledgered with trigger) · E7 (`set_status` name kept; the
   C-battery is the measured re-open trigger) · E8 (unbounded pending read → findings
   row, owner packet 05) · E9 (#147 closed at exit by the deploy smoke). Recommend:
   ratify as a block.

---

## §P Probe appendix (all fresh, this session, 2026-07-24 at `29708f5`)

- **P1** `grep` over `loremaster/tests/test_comms_promise_registry.py`: committed
  vocabulary lines 165–244; fleet marker `f"{_EM_DASH} re-run with limit=5"` (≈1017);
  drain elision proof fixture `entries=[61,62], total_pending=7, limit=2` (≈1044–1055);
  ACK-REQUIRED marker `"ACK REQUIRED: #71"` + directive/signal proof legs (≈1057–1068);
  `_p03_entry(*, seq, grade="signal", acked_at: Any = None)` (≈736).
- **P2** `" ": "join separator"` present in `_PROMISE_FREE` (≈249) — the AC-02 control.
- **P3** reach pin body (≈387–410): `missing = expected - observed` → FLOOR, not
  exact-set; canonicaliser comprehension handling (≈1542–1553): opaque-or-denied.
- **P4** `test_message_ledger.py::_ask` (≈2066): `ledger: Any`, declared `-> int` — the
  no-any-return pair's shape; total 36 per the lead's reset verification.
- **P5** `docs/reference/surrealdb-31-capabilities.md`: `object::extend` working-shape
  (≈292) and `SELECT *` NONE-omission (≈302–307, §6.6 item 12) present at HEAD.
- **P6** asyncio probe, 3 legs: finally-shape on CANCEL → `['finally-arm']`; WB30-shape
  on CANCEL → `[]`; finally-shape on SUCCESS → `['finally-arm']` (positive control).
- **P7** installed SDK: `streamable_http_manager.py:268 new_session_id = uuid4().hex` —
  the transport correlator cannot recycle.
- **P8** `test_comms_tool.py` ≈4174–4182: `drain_limit` driven through config — the
  committed force behind B6.1.
- **P9** `test_surreal_schema.py` ≈1249/1253: `"TYPE int" … hit_count`,
  `"TYPE string" … session`; `test_surreal_fakes.py` ≈925–936: exact 7-name signature
  list — the two AC-16 collateral REDs.
- **P10** `test_surreal_harness.py` ≈1761: the derived-counts pin, docstring baseline
  35/21.
- **P11** production corpse grep: `surreal.py` 747/764–765 (six-core, fire-and-forget);
  `server.py` 1640–1642 (no-caller-yet-wires); `surreal_schema.py` 116/949/952
  (six-core×3).
- **P12** `test_message_ledger.py` 2240/2302: both R2 self-addressed pins present.

*Line numbers above are read-session locators only; every citation that must survive
edits names the symbol or test.*
