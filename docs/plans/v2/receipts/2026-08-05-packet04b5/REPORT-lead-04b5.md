# REPORT-lead-04b5 — wave close-out (packet 04b5, INJECTION #321 Link-5 render-site containment)

lead-base v3 read

## SUMMARY BLOCK
| field | value |
|---|---|
| `Verdicts acted on:` | `4c5930d→HEAD SAME` (coldaudit-contract-1 INSUFFICIENT) · `4c5930d→SAME` (coldaudit-4 INSUFFICIENT, render) · `9cb1633→SAME` (coldaudit-5 INSUFFICIENT, P-F/P-S) · `f11fc67→SAME` (recheck NO-GO §SAT) · `980937d→SAME` (build coldaudit NO-GO ruff). **No STALE acted on** — every graded SHA was an ancestor of HEAD when acted on. |
| `Directives:` | `1 ledger / ~6 wake / 1 prose-duplicated` — ⚠ the 1 duplication was the go-signal to coldaudit-04b5-3 (content on native FIRST, corrected to lore_comms #3012 + native wake after operator feedback; the durable-channel lesson). |
| `Rulings:` | `9 in a committed artifact (packet file 04b5-injection-containment.md: DOOR SCOPE, FENCE UNIFY, coverage.py, code-RAG OUT, self-echo route, .format close, + lead rulings) / 0 body-only` |
| `Agents:` | `17 spawned / 17 ledger-retired / 0 left running` (verify: fleet shows only lead-04b5) |
| `Uncommitted at stop:` | `0 files` |

## THE WAVE (property-to-invent → contained)
#321: caller free-text reaching another agent's served answer as lore's own voice (a measured
prompt-injection class). The fix — the render seam `render_attributed`/`fence_width` — routes EVERY
caller-origin byte reaching a served answer through provenance-delimited containment, enforced by
reach nets that are **checked variables**, not hand-lists.

- **Contract phase (deep):** 5 contract authors (two lost to transient API errors, both recovered
  from committed checkpoints — the lesson: checkpoint every round), a Fable design sidecar (the
  two-waves tripwire fired on the render-reach property), and Fable delta-audits. The render-half
  reach instrument was rebuilt to the sidecar's §4 pins (P-U interpolation-property universe,
  NOT the `_render_` prefix — itself a six-defeats name-list; P-F model-guarded field manifest;
  P-S coverage.py branch observation). Two dead checked-variables (P-F tautology, P-S unsatisfiable)
  caught by delta-audit + fixed.
- **Recheck (Opus 4.8, operator-directed):** caught a fabricated §SAT receipt (claimed filled,
  produced by nobody). Operator ruled the production build's green-contract gate discharges #133.
- **Build:** the seam + full-derived door routing (49 error doors + ~40 renders + memory.text fenced
  + search.py full fence route-through + R3 pin ripple). Cold audit **GO**: 0 leaks over 12 hostile
  forgery shapes, mutation-proven non-vacuous, f-string-embed byte-safety proven by construction.
- **Two operator all-or-nothing extensions:** 18 bare-`ValueError` self-echo doors routed
  (Class-1 scan-enforced, Class-2 bounded-pin); and the **`.format()` reach blind spot closed** — a
  real completeness gap both the recheck and cold audit missed (they tested routed sites, not the
  reach net's interpolation-mechanism coverage), exposing a live self-echo door (caller_model).

## RULINGS (all in the committed packet file + resolved findings)
Operator: DOOR SCOPE=FULL derived · FENCE UNIFY=full route-through · branch coverage=coverage.py
(installed) · code-RAG file renders=B-5 OUT (→pkt39) · self-echo doors=route (literal all-or-nothing)
· .format() gap=close. Lead: ruff RED→FIX (not adjudicate) · impact.py:819=B-5 OUT (resolved indexed
identifier) · enforcement fork=Class-1 scan + Class-2 bounded pin.

## GATES (lead-verified, close-out)
- 04b5 contract (`test_link5_render_containment.py` + `test_task_read_surface.py`): **256 passed / 0 failed**.
- ruff: **clean tree-wide**. Currency gate: **PASS** (typecheck/pytest RED_ADJUDICATED to packet-39; ruff GREEN; **zero RED_ORPHANED**).
- Full-suite regression: all 444 failures in auth-WIP files (pkt 39/45/48/49, #333) — **ZERO in any 04b5-touched file**; 04b5 adds zero regressions. (The 316→444 vs the 04b4-recorded count is the auth-WIP suite's full-manifest scope, all owned.)
- Cold audit GO on containment-in-bytes; build non-vacuous (mutation-proven).

## DEPLOY / RESIDUALS
- **COMMIT-ONLY** (rides 05a's deploy). ⚠ **04b5 MUST DEPLOY before pkt 56** (#321's re-open trigger).
- #321 RESOLVED (deploy-gated note). #335 (.format blind spot) RESOLVED (closed). Filed by closers,
  routed. impact.py:819 ratified B-5 OUT.
- Commits: `4c5930d`→`6ff51cf` (10). No new resume docs. Reports archived → `receipts/2026-08-05-packet04b5/`.
