brief-base v6 read

# REPORT — design-sidecar-03b-1 · deferred design rulings for packet 03b

## SUMMARY BLOCK

- `brief-base v6 read` · store reference read (§1.1/§1.4/§1.5/§1.6/§2/§5/§10) — cited, never re-transcribed.
- state: **done** (design doc written; standing by for follow-ups per brief).
- deviations: (1) lore tools loaded via ToolSearch but navigation used grep/Read — every question was a known-path read, a cross-cutting map over a known diff, or a non-symbol textual seam (the three documented-legitimate fallbacks; said out loud per dogfood §3). (2) No live probes run; every engine claim cites the store reference.
- decisions-needed (operator): **(a)** confirm the fix-wave batch in the placement map — the one DEPLOY-GATED item is DD-1.a (`trace_ts` index; the free window closes at this deploy); **(b)** DD-1.b placement fork (windowed aggregate: fix wave vs first follow-up); **(c)** DD-3 placement fork (pointer bounds: fix wave [recommended, free-window] vs 05); **(d)** ratify the three widened 05 decision points (DD-2.a, DD-4.c, stamped_seqs) and 06's two (DD-1.c retention, DD-5.b join receipt).
- deliverable: `docs/plans/v2/03b-deferred-design-rulings.md` (DD-1…DD-6 + consolidated placement map).
- receipt pointers: DD-1 (D6: retention/scan/cardinality) · DD-2 (D7: the debt machine) · DD-3 (D8: refs bypass) · DD-4 (D11: at-most-once) · DD-5 (Q1: does the instrument answer 06) · DD-6 (folded Q2/Q3/Q5/Q7/Q8/Q9 + cold R1/R2/R7/R10, one verdict each).
- All claims dated 2026-07-25 at working tree `8be70b7` (fix wave in flight; `_TRACE_BY_TOOL_CAP` already present at my read).

---

## One recommendation per item (the brief's required shape)

| item | recommendation | cost | the wrong build it stops | packet |
|---|---|---|---|---|
| **1. Trace retention + scan (blind D6)** | THREE-PART SPLIT. **DD-1.a**: `trace_ts` plain index NOW, in the schema free window (r2 §T2.1's own argument — later it builds blocking over weeks of rows, §1.5). **DD-1.b**: `trace_aggregates` gains `WHERE ts > $cutoff` (config window, default 14d; served prose derives the window). **DD-1.c**: row GC **deferred to 06** — the rows ARE 06's instrument; deleting before the curve is read is tuning ahead of the measurement. Mechanism specced (bounded DELETE on the reconcile tick, never in boot), retention ruled at 06's close-out (recommend 90d), findings row files the deferral, escalation trigger at 1M rows. | index: 1 line + 1 pin. window: ~1 conjunct + config + derived prose + 3 pins + an EXPLAIN receipt. GC: 06's build. | a render-windowed-but-scan-unbounded build (EXPLAIN receipt + query pin); an "every tool ever" label over windowed numbers (derived-label pin); a retention sweep that eats 06's data (the deferral) | **DD-1.a: fix wave (deploy-gated)** · DD-1.b: fix wave or first follow-up (fork) · DD-1.c: 06 |
| **2. The taught-but-unserved debt machine (blind D7)** | Mechanism fully designed so 05 implements rather than invents: a `waiting:` line on drain+heartbeat via ONE shared helper (the B4.1 skew precedent — sharing proven by mutation), emit iff `awaiting_answer` is non-None, prose derived from the typed `WaitingOnAnswer`; `await`'s timeout render is the natural first consumer; fleet gets NO per-row cell (an N×2-query N+1) — a batched ledger read if 05 wants it. **Ownership: 05**, and its existing R1 clause WIDENS: use the mechanism or adjudicate the whole thing (method+model+index+narrowing) for removal — a third deferral is an operator fork. Plus **DD-2.b, fix-wave**: pin `message.thread` as required non-option `string` (Q9's silent dependency of the R3 superset proof). | 05: one template + helper extraction + proofs + battery case. 03b: one schema pin. | a 05 builder wiring "waiting" to the STORED `input_required` status (vocabulary conflation — discriminating pair kills it); a future `option<string>` thread silently dropping answers | 05 (mechanism) · **fix wave (DD-2.b pin)** |
| **3. `refs` bypasses the body cap (blind D8)** | Finish the body cap's own rationale: `MESSAGE_POINTER_MAX_CHARS = 256` on each ref, `thread`, `task_id`; `MESSAGE_REFS_MAX_COUNT = 20`; `note` caps at the BODY constant (it is prose, not a pointer). Enforced at BOTH layers mirroring `body` exactly — ledger teaching reject (the ONE writer) + store ASSERT backstop (per-entry array-ASSERT syntax verified against surrealql-tests first; honest count-only asymmetry if the engine lacks it). The `refs` description is REWRITTEN to teach the bounded pattern with derived constants — today it recommends the bypass verbatim. NO charset on thread (settles Q8: it is bound-param everywhere and the taught `q:<topic>` contains `:`, which the identity charset forbids). | 2 constants + 1 teaching error + ASSERTs + description rewrite + pins + one battery re-run (C3's trigger) | a 100 KB payload riding one ref verbatim into a recipient's context, silently voiding B6.3's render arithmetic; truncate-instead-of-reject; ledger-only enforcement a future writer bypasses | **fix wave (recommended — the message-table free window closes at this deploy)**; 05 is the priced alternative |
| **4. `drain` at-most-once (blind D11)** | Decomposed: the render leg is SYNC after the stamp (skew reads before drain — cold D3), so the real loss legs are transport/cancel-at-response, which no in-process reorder closes. **DD-4.a**: do NOT reshape drain in 03b (an oracle reshape buying only the rare pinned leg). **DD-4.b, fix-wave**: pin "no awaited work after `ledger.drain()` returns" (cold D3's ordering pin, widened to the loss-window property). **DD-4.c**: 05's `since=` MUST serve SEEN rows — that requirement is what converts loss to recovery; if 05 declines, D11 returns to the operator. **DD-4.d**: exposure is MEASURED — `ok=false` drain trace rows are the loss-rate upper bound 06 reads; re-open trigger for a lease/at-least-once shape: >0.5% or any confirmed lost directive. **DD-4.e**: peek stays the manual mitigation; do NOT teach peek-first (doubles traffic, reintroduces remember-to-stamp decay). Honesty fix: rows are stamped, not destroyed — "permanent" is a property of the verb set, and 05 fills exactly that gap. | one structural pin + docstring lines now; the recovery rides work 05 already declared | a refactor that quietly inserts an await between stamp and return (the pin); a 05 `since=` that serves only unseen rows (the named requirement); panic-building a lease nobody measured a need for | **fix wave (DD-4.b pin)** · 05 (DD-4.c) · 06 (DD-4.d reads) |
| **5. Q1 — does the telemetry answer 06? (rated most consequential)** | **Honest answer: yes to the QUESTION, not to the ideal CURVE — by deliberate refusal.** Per-agent numerator complete; per-agent denominator only via the transport-session join, which likely degrades one-to-many (shared MCP connection) — VISIBLY, as T4.4 designed. Inference-free per-agent instruments that survive any join quality: drain gaps in wall-clock + fleet-ordinal terms, and the register-then-never-drains population (total decay, exact, per agent). **DD-5.a**: all side doors stay shut — sticky attribution (MP9, re-affirmed), an `agent=` param on every tool (a sometimes-filled identity is WORSE than none: it measures diligence, not decay — the decaying population is exactly the one that stops filling it), `_meta` plumbing (absent in practice). **DD-5.b**: 06 opens with a JOIN-QUALITY RECEIPT — one SELECT during the drill measuring agents-per-transport-session (needs a one-line 06 packet-file edit — lead's, outside my writable set). **DD-5.c**: decay ⇒ gaps grow / drains stop, both visible regardless — what the fallback loses is rate precision, not the phenomenon. | one SELECT at 06 entry + one packet-file line | a 06 author who, finding the curve coarse, quietly resurrects the refused inference to smooth it | 06 (no 03b work) |

**Folded small items (DD-6, one verdict each):** Q2/cold-R2 → packet 04's counts family (already routed by r2 §A-GRAFT) · Q3 → affirmed as-is (r2 §F.1) · Q5 → KNOWN BOUND, docstring + re-open trigger; `stamped_seqs` becomes true or dies in 05 · Q7/cold-R3 → deploy runbook: read-only counts on `:18500` BEFORE DDL, non-zero = STOP · Q8 → settled (DD-3.e) · Q9 → settled (DD-2.b) · **cold R1 → T5.2 AFFIRMED (awaited-inline), plus the two halves it was missing: the docstring disclosure, and an INSTRUMENT for the >5%-p50 trigger (the deploy smoke times a read-tool batch; this deploy's figure is the baseline)** · cold R7 → findings row, owner 05 (E8 affirmed) · cold R10 → ruled value affirmed, trigger CORRECTED honestly (the generic seam cannot see drains-at-the-cap — the honest triggers are a battery fixture at the cap and filed findings; no drain-specific declared key).

## Fix-wave-eligible vs real packets

- **Cheap enough for this packet's fix wave** (each additive, mechanical, mutation-provable — the standing authorization's three conditions; operator confirms the batch): DD-1.a (**deploy-gated**), DD-2.b, DD-4.b, the DD-4/Q5 docstring lines, cold-R1's disclosure line. DD-1.b and DD-3 are slightly larger but ride the same free-window/pre-deploy logic — both flagged as operator forks with fix-wave recommended.
- **Real packet work, not drive-bys**: DD-2.a's waiting line (promise-registry + proofs + battery = contract-author + adversary, 05) · DD-1.c's GC (06, after the curve) · DD-4.c (05's since= design) · DD-5.b (06 entry).

## Genuine forks flagged (none silently chosen)

1. DD-1.b placement (fix wave vs follow-up) — the scan only grows into a problem over weeks; only the index is deploy-gated.
2. DD-3 placement (fix wave vs 05) — the free window and the bypass-teaching description both argue for now; the counter-argument is fix-wave scope discipline.
3. DD-2.a's priced alternative (03b ships the waiting line) — recommended NO, but priced so the operator can overrule.
4. The three widened successor decision points (DD-2.a use-or-remove, DD-4.c seen-rows requirement, DD-1.c retention) — each carries the named escalation if its packet declines.

## Self-attack summary (per the brief's requirement)

Every DD section carries its own "wrong build stopped" analysis inline; the ones that changed my design while writing: (a) DD-1's first draft had a retention sweep in 03b — killed by measure-then-tune (the rows are 06's instrument); (b) DD-4's first draft favored stamp-after-render — killed by decomposing the loss legs (the reshape buys only the sync-render leg, which is a code-defect leg, not an operational one); (c) DD-5's `agent=`-param option looked cheap until the bias attack (a sometimes-filled identity measures diligence, not decay); (d) DD-3's per-entry store ASSERT is stated as verify-first because I could not confirm the array-closure syntax from the store reference alone and did not probe.

## What I did NOT do

No production/test edits; no container touched; no git mutation; no `:18500` read; `/home/ejprice/scratch/**` and the tainted tag untouched; no probes (all engine claims cite the store reference). The 06 packet-file edit DD-5.b needs is FLAGGED here, not made (outside my writable set).

**Standing by for follow-ups.**
