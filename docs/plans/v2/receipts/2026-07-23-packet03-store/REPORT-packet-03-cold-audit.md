# REPORT — packet-03-cold-audit

brief-base v6 read

## SUMMARY BLOCK
- verdict: **GO**. The uncommitted change to `loremaster/loremaster/store/surreal_schema.py` is correct, in-scope, and every load-bearing pin is mutation-proven to discriminate. All numbers below were RE-RUN and RE-DERIVED by this audit, not copied from the builder.
- state: **done** — independent cold audit complete; nothing edited except three scratch mutations, each restored byte-exact (md5 `3ce3078a6acb57ce01d21d4f22630884` throughout).
- gates (my re-runs): scoped **304 passed/0 failed**; blast-radius **432 passed/0 failed**; 03a `test_message_ledger.py` **RED 14 failed/166 errors — byte-identical to HEAD**, all `ModuleNotFoundError: loremaster.messages`; mypy **54 errors, ALL in test files, 0 in any production file** (HEAD had 55; the change RESOLVED exactly one — `test_surreal_schema.py:425 generate_message_ddl`); ruff clean.
- mutation matrix: 3 independent wrong-builds each RED on the named pins + a 13-pin correct-build positive control GREEN on restore. Real-tree mutation; provenance receipt below.
- edge directions: all four independently confirmed against the real RELATE call sites (`briefs.py`, `graph_surreal.py`) / the 03a contract fan-out (`to`).
- deviations verified against the CONTRACT (not spec prose): (a) `asked_at` derived, no stored column — correct; (b) `MESSAGE_BODY_MAX_CHARS==2000` equals the 03a contract literal; `question bool DEFAULT false` matches the non-optional ruling-9 field.
- decisions-needed: **one, the builder's** — `scripts/typecheck.sh` is not globally zero (54 errors in the immutable RED 03a/03b contract files). This is inherent to the TEST-ONLY multi-packet split, not a defect in this change. Lead/operator ruling on whether the packet-03 exit gate is "clean writable file + no new errors" (which is satisfied) vs "global zero" (impossible until 03a/03b build).
- receipt pointers: gate tails §Gates · mutation matrix §Mutations · directions §Edge directions · deviations §Deviations · residuals §Residuals.
- provenance (real-tree mutation runs): `loremaster.__file__ = /home/ejprice/PycharmProjects/lore/loremaster/loremaster/__init__.py` (the real tree, NOT site-packages — #140 guard).

## What is under audit
`git diff --stat`: `loremaster/loremaster/store/surreal_schema.py` ONLY (220 insertions, 6 deletions). `git status`: that file modified + the two REPORT files. No stray files. `generate_message_ddl`/`_message_statements` have **zero production consumers** — defined in `surreal_schema.py`, called only by `test_comms_schema.py` + `test_surreal_schema.py`, and NOT wired into any live boot aggregation. Scope (STORE+TEST, NO deploy) is respected: the production-touching `OVERWRITE` flip is committed to source and deploys at 03b, not here.

## Gates (re-run 2026-07-23, spike-surreal `ws://127.0.0.1:18000` 3.2.1; production `:18500` never touched)
| gate | command | my result |
|---|---|---|
| scoped | `pytest test_comms_schema.py test_surreal_schema.py -n auto` | **304 passed, 0 failed** in 13.88s |
| blast radius | `pytest test_graph_surreal.py test_brief_ledger.py test_agent_registry.py test_comms_render_architecture.py test_comms_fleet_grouping.py -n auto` | **432 passed, 0 failed** in 14.00s |
| invariant (03a MUST stay RED) | `pytest test_message_ledger.py` | **14 failed, 166 errors** — all `ModuleNotFoundError: No module named 'loremaster.messages'`. RE-DERIVED IDENTICAL at HEAD (`git stash`ed the change → same `14 failed, 166 errors`). Unchanged: a build that greened any of these would be a scope breach; none did. |
| mypy | `scripts/typecheck.sh` | **54 errors, 4 files, ALL under `loremaster/tests/`** — `test_comms_tool.py` (32), `_message_fakes.py` (12), `test_comms_promise_registry.py` (7), `test_message_ledger.py` (3). **ZERO in `surreal_schema.py` or any production file** (grep-confirmed). |
| ruff | `uv run ruff check .` | **All checks passed!** |

**mypy claim independently verified by the strongest instrument — a HEAD diff.** I `git stash`ed the one changed file and re-ran `typecheck.sh` at clean HEAD (`f0b0d9e`): **55 errors in 5 files** — the same four test files PLUS `test_surreal_schema.py:425` (`Module "loremaster.store.surreal_schema" has no attribute "generate_message_ddl"`). The change's error set is a **strict subset** of HEAD's: it adds zero, and resolves exactly one — the contract pin that references the new `generate_message_ddl` symbol. The claim "0 in production, count did not increase over 55" holds (it decreased by one, legitimately). Restored the file; md5 back to `3ce3078a...`.

## Mutations (real tree, `cp -a` content backup, each restored + md5-verified byte-exact; I built my OWN wrong versions, did not trust the builder's matrix)
| # | my mutation | pin(s) it must RED | result |
|---|---|---|---|
| A | `to` guard → `IF NOT EXISTS` (kept IN/OUT/ENFORCED), via a `name=="to"` branch in `_define_relation_table` | `TestRelationFlipAgainstAnExistingStore::test_the_guard_is_LIVE_after_applying_the_new_ddl_to_a_dirty_store` (the #107 silent-no-op catch) + `TestMessageDdlOffline::test_defines_the_to_edge_as_a_typed_relation_table` + guard-kind pin | **3 RED**. The dirty-store pin failed because on an existing table `IF NOT EXISTS` is a silent no-op → ENFORCED never lands → ghost RELATE succeeds. The guard-kind pin named the offender: `'DEFINE TABLE IF NOT EXISTS to TYPE RELATION IN message OUT agent ENFORCED SCHEMAFULL'`. |
| B | drop `ENFORCED` (`enforced=True`→`False` at the `to` call site) | `TestEnforcedIsLiveOnTheDeliveryEdge` | **4 RED** (`test_a_ghost_OUT_endpoint_is_REJECTED`, `_ghost_IN_`, `test_no_dangling_edge_survives_a_rejected_relate`, `test_ENFORCED_closes_the_INSERT_RELATION_door`: all "DID NOT RAISE") + **2 CONTROLS stayed GREEN** (`test_CONTROL_a_real_endpoint_pair_is_accepted`, `test_CONTROL_INSERT_RELATION_is_legal_with_real_endpoints`). The pins discriminate on ENFORCED alone. |
| C | `refers` → bare untyped `DEFINE TABLE IF NOT EXISTS refers TYPE RELATION SCHEMAFULL` | guard-kind pin + `TestRelationTablePolicyFlip::test_every_relation_table_in_every_comms_generator_is_typed` (∀) + `TestTheFlipAgainstTheTablesThatACTUALLYEXISTInProduction::test_the_flip_LANDS...[refers]` (dirty-store vs 101,479 prod rows) | **3 RED**; the sibling params `[briefed]`/`[answers_to]` stayed GREEN — the mutation is isolated to the mutated table, so the pin discriminates per-table. |
| control | restore byte-exact | all 13 mutated pins | **13 passed** on the restored tree — the positive control that proves the pins are green on the correct build, not merely un-fireable. |

**Guard-kind pin vacuity — interrogated and cleared.** `_generated_ddl()` in `TestFieldDdlConvergesOnAnExistingStore` includes `generate_agent_ddl`/`generate_brief_ddl`/`generate_graph_ddl`/`generate_message_ddl`, all of which emit `TYPE RELATION` tables, and the test asserts `seen["TABLE (RELATION)"] > 0` (fails-blind otherwise). The parser reclassifies a `DEFINE TABLE ... TYPE RELATION` to kind `TABLE (RELATION)` (required guard `OVERWRITE`) — proven live: mutations A and C both drove it RED with a `IF NOT EXISTS ... TYPE RELATION` offender. Not a pass over an empty category.

## Edge directions (each confirmed against the ACTUAL RELATE site — a reversed IN/OUT would reject existing prod rows at 03b's deploy)
| edge | diff emits | RELATE site (independently read) | match |
|---|---|---|---|
| `briefed` | `IN agent OUT brief` | `briefs.py::_relate_briefed` and `publish`: `RELATE $from->briefed->$to` with `from=RecordID(AGENT_TABLE)`, `to=RecordID(BRIEF_TABLE)` (both sites, :760/:919-920) | ✅ |
| `refers` | `IN code_node OUT name` | `graph_surreal.py::_edge_statement`: `RELATE $src->refers->$dst`; `src=_code_node_id→RecordID(CODE_NODE_TABLE)`, `dst=_name_id→RecordID(NAME_TABLE)` | ✅ |
| `answers_to` | `IN code_node OUT name` | `graph_surreal.py::_node_statements`: `RELATE $id->answers_to->$fqn_name`/`$bare_name`; `id=_code_node_id→code_node`, both name keys `=_name_id→name` | ✅ |
| `to` | `IN message OUT agent` | 03a prod site not built (`loremaster.messages` absent — correctly RED); the contract's `_relate_to` pins the intended shape: `RELATE $from->to->$to`, `from=RecordID(MESSAGE_TABLE)`, `to=RecordID(AGENT_TABLE)`. Matches store-ref §4's `message->to->agent` fan-out (out = caller-supplied recipient). | ✅ |

`ENFORCED` is on **`to` ONLY** (grep: `enforced=True` at exactly one call site, line 1270); `briefed`/`refers`/`answers_to` pass no `enforced=` → default `False` → not ENFORCED. Correct per scope — enforcing an edge that already carries dangling rows is a distinct hazard, and the pre-flight audit did not clear those three for it.

Dirty-store migration pins are genuine (apply OLD-untyped → NEW, assert guard LIVE + old row SURVIVES) and each carries a positive baseline control: `test_BASELINE_the_old_world_really_is_unguarded` (proves the old world accepted a ghost) and `test_the_flip_LANDS...`'s `assert "IN " not in before` (proves the table started untyped) + `test_CONTROL_a_HOMOGENEOUS_row_stays_fully_writable` (isolates the write-poison hazard to the endpoint TABLE, one variable).

## Deviations (verified against the contract, not the packet prose)
- **(a) `asked_at` is DERIVED, no stored column.** `grep asked_at surreal_schema.py` → only comments; `_MESSAGE_FIELD_SPECS` has no `asked_at`. The 03a contract `test_asked_at_IS_the_questions_own_created_at` pins `waiting.asked_at == sent.message.created_at`. The spec prose (`03-comms-message-graph.md:45`) lists `asked_at` as a node field — that prose is imprecise; the builder correctly followed the contract. **Correct call.** A stored required `asked_at` would poison the contract's own `_create_message` (which omits it); an `option<>` one would be dead.
- **(b) `MESSAGE_BODY_MAX_CHARS = 2000`.** The store constant (`surreal_schema.py:566`) drives the body ASSERT `string::len($value) <= 2000` and is the single source 03a is instructed to import (DRY). The 03a contract asserts `MESSAGE_BODY_MAX_CHARS == 2000` (`test_message_ledger.py:349`). Store value == contract literal. `question bool DEFAULT false` matches the non-optional `Message.question` (ruling 9); `DEFAULT false` is implicitly exercised by every live `_create_message` that omits it and succeeds.

## RESIDUAL table (each concern individually; "all remaining are X" is banned)
| # | concern | file:symbol | verdict |
|---|---|---|---|
| R1 | Sequence carries no `BATCH`/`START`; a changed one would never migrate onto an existing store (#146). | `surreal_schema.py::_define_sequence` | **Accepted-with-trigger, correctly.** No `BATCH`/`START` is emitted, so nothing to migrate today; named re-open trigger = "the day either is added" (#146). The catastrophic case (re-apply resetting the counter) IS pinned live: `test_the_slice_is_idempotent` asserts `nextval` advances across a re-apply. Satisfies the spec's #146 adjudication (`03-comms-message-graph.md:96-98`). Not a defect. |
| R2 | The `[real]`-tier ledger leg is UNGRADED (adversary §7.8, "the largest residual risk"). | `docs/plans/v2/03-comms-message-graph.md:33` | **Out of packet-03 scope, correctly scoped.** That leg is 03a's `MessageLedger` real-store tests, which cannot exist until `loremaster.messages` is built. Packet 03's OWN `[real]` store pins (`TestMessageSchemaLive`, `TestEnforcedIsLiveOnTheDeliveryEdge`, `TestRelationFlipAgainstAnExistingStore`, `TestTheFlip...`) all ran LIVE against 3.2.1 and passed — the store's real tier IS graded here. This is why 03/03a/03b each ship with their own cold audit. |
| R3 | `question` and `ack_note` field TYPES are not individually pinned in the OFFLINE contract (the `test_message_field...` parametrize omits both; `to`.`session`/`created_at` likewise). | `test_comms_schema.py::TestMessageDdlOffline` | **Minor test-coverage residual, not a defect.** Both fields ARE declared correctly and ARE covered by the ∀-guard pin (`test_every_field_uses_the_overwrite_guard...` → OVERWRITE). `question`'s `bool`+`DEFAULT false` is implicitly proven (every live `_create_message` omits it and succeeds; a wrong required-bool would raise). `to`.session/created_at are exercised by the live `_relate_to`. `ack_note` and a `question=true` store round-trip are 03a's behavioral surface. Flagging so it is met deliberately, not silently inherited. |
| R4 | `scripts/typecheck.sh` is not globally zero (54 errors). | 03a/03b immutable contract test files | **Decision-needed for lead/operator, not a NO-GO.** Zero in the writable file and zero in any production module; count decreased 55→54. The residual is structural to the TEST-ONLY multi-packet split (contract references not-yet-built `loremaster.messages`/`_render_comms_*`). Needs a ruling on the packet-exit gate definition; does not block the store work. |
| R5 | The `OVERWRITE` flip on `briefed`/`refers`/`answers_to` is production-touching and lands on the NEXT deploy (03b), against 101,479 live edge rows. | `surreal_schema.py::_define_relation_table` callers | **Adjudicated safe by the contract's dirty-store pins**, which apply OLD→NEW on an existing table and assert the guard lands + rows survive (store-ref §1.5: `DEFINE TABLE OVERWRITE` does not rebuild, preserves rows). Not deployed in THIS packet. The write-poison hazard for a heterogeneous endpoint is pinned (`test_a_HETEROGENEOUS_row_is_write_poisoned_not_dropped` + homogeneous control), so the pre-flight "production is homogeneous today" premise is guarded behaviour, not an unpinned assumption. |

## Tool honesty
lore MCP tools were loaded and available; I used direct `Read`/`Grep`/live-probe for the audit because this is a RENAME/signature-change EXHAUSTIVENESS question (one missed `_define_relation_table` caller compiles-but-mypy-errors) and a natural-language/direction verification — the three cases repo law reserves for grep over the graph. `grep` found exactly 4 call sites, all in-module; mypy's clean production pass confirms none were missed. Edge directions were read from the real `briefs.py`/`graph_surreal.py` sources, not inferred. Every gate number and mutation verdict is from a live run against the real tree / spike-surreal 3.2.1, re-derived independently of the builder's report (which I read LAST, after forming this view — it agrees on every material point).

## Restore receipt
Three scratch mutations, each applied to the real tree and restored from a `cp -a` content backup (`/tmp`, now removed); md5 verified `3ce3078a6acb57ce01d21d4f22630884` after each restore and at report time. `git diff --stat` = only `surreal_schema.py` (220+/6-). Tree left exactly as found (modified `surreal_schema.py` + the two REPORT files). Did NOT commit.
