brief-base v14 read
brief project v7 read

CAPABILITY CHECK: my model was ASSERTED by the brief as `claude-opus-4-8`; I cannot
self-read the running model id, so I confirm the brief's assertion rather than the fact.
All tools the brief demanded were available and used (Read/Edit/Write/Bash, `lore_comms`
register+heartbeat, `lore_claim_task`/`lore_tasks`, the deferred lore MCP surface via
`ToolSearch "+lore"`). No unmet demand — nothing blocked.

## SUMMARY
- state: **done-with-deviations**
- what: added the `lore-adm` console-script entry point (operator ruling 2026-08-20) so the
  admin CLI is invoked as `lore-adm`, not `python -m loremaster.principals`.
- deviation 1: ran `uv sync --all-packages`, NOT plain `uv sync`. The root is a VIRTUAL
  workspace root (`package = false`); plain `uv sync` there uninstalled all 118 member
  packages and did NOT create `lore-adm`. `--all-packages` is the correct invocation (it
  matches the Containerfile + gates). Venv left healthy (loremaster importable, 45 tests pass).
- deviation 2 (in-scope prose): fixed TWO docstrings that taught the retired prog
  (`build_parser` docstring + the prog-pin docstring) — repo P8d law (prose↔code consistency).
- deviation 3 (not mine): 3 DOCS are dirty in the working tree and I did NOT touch them —
  `docs/design/2026-08-20-packet49-cli-keys.md`, `docs/plans/v2/49-principal-cli-keys.md`,
  `docs/plans/v2/INDEX.md` (pre-existing in-flight edits). My code diff is exactly 3 files.
- Packages considered: console-script entry point = standard PEP 621 `[project.scripts]`,
  generated by the `hatchling` build backend (VERIFIED: `.venv/bin/lore-adm` created by
  `uv sync --all-packages`) — not hand-rolled. Resolution pin uses stdlib `tomllib` +
  `importlib` to parse+resolve the target — `replace` (no hand-rolled TOML/entry-point parse).
- Reuse ledger: none — no new reusable production symbol (a `_CLI_PROG` value change, a
  pyproject entry, and one new TEST method; a test is not a reusable symbol).
- Graded: n/a — this is a build report; it renders no verdict on another agent's artifact.
- decisions-needed: none. `uv.lock` is UNCHANGED (md5 identical before/after; `git status`
  clean) — no lock-commit decision for the lead.
- receipt POINTERS: change → §The change; `lore-adm` receipts → §lore-adm receipts;
  gate tails → §Gates; registration_sites → §registration_sites.

## The change (3 files, git diff HEAD)
1. `loremaster/pyproject.toml` — added a `[project.scripts]` table (placed after the
   `[project]` `dependencies` close, before `[tool.uv.sources]`, per the file's section
   ordering; other tables untouched):
   `lore-adm = "loremaster.principals:main"`.
2. `loremaster/loremaster/principals.py` — `_CLI_PROG` changed
   `"loremaster.principals"` → `"lore-adm"` (the prog users see in `--help`/usage/error
   prefixes; it drives `prog=_CLI_PROG` at `build_parser` and the two stderr error prefixes).
   `main()` and the `__main__` guard are UNCHANGED (guard stays LAST) — so
   `python -m loremaster.principals` still works as a harmless fallback. Also fixed the
   `build_parser` docstring line that named the retired prog (P8d prose↔code consistency).
3. `loremaster/tests/test_principals_cli.py`:
   - (a) UPDATED the existing prog pin
     `TestParserSurface.test_build_parser_prog_is_the_fixed_invocation` to assert
     `prog == "lore-adm"` (+ its docstring). No other frozen pin touched.
   - (b) ADDED the resolution pin `TestConsoleScriptEntryPoint.
     test_lore_adm_entry_point_resolves_to_a_real_callable` (+ stdlib `importlib`/`tomllib`
     imports): reads `pyproject.toml` via `tomllib`, asserts
     `[project.scripts]["lore-adm"] == "loremaster.principals:main"`, then RESOLVES that
     declared target exactly as the console-script machinery does
     (`importlib.import_module` + `getattr`) and asserts it is callable — so an entry point
     naming a nonexistent module/attr (a `lore-adm` that would ImportError on first run)
     reddens here.

### Prog-pin discrimination (not vacuous)
The updated pin genuinely discriminates: with the code at `_CLI_PROG = "lore-adm"` and the
test asserting `== "lore-adm"`, reverting EITHER side alone reddens it (a prog/assert
mismatch). Live corroboration below: `usage: lore-adm ...` in `--help`, and
`lore-adm: error: ...` on the bare invocation.

## lore-adm receipts
`uv sync --all-packages` → SYNC_EXIT=0; `Built loremaster @ .../loremaster`;
`.venv/bin/lore-adm` created (shebang `#!/home/ejprice/PycharmProjects/lore/.venv/bin/python3`).

```
$ .venv/bin/lore-adm --help          # HELP_EXIT=0, clean stderr
usage: lore-adm [-h] [--config CONFIG]
                {add,list,delete,suspend,unsuspend,set-expiry,mint-key,revoke-key,list-keys} ...

Manage lore principals (human identities) and their per-user API keys. Every
verb executes directly; only `list`/`list-keys` are reads.
  add / list / delete / suspend / unsuspend / set-expiry / mint-key / revoke-key / list-keys

$ .venv/bin/lore-adm                 # BARE_EXIT=2 — dispatch confirmed (usage on stderr)
usage: lore-adm [-h] [--config CONFIG]
                {add,list,delete,...} ...
lore-adm: error: the following arguments are required: command

$ .venv/bin/lore-adm list --help     # LIST_HELP_EXIT=0 — subcommand dispatch
usage: lore-adm list [-h]
```

## Gates
- `uv run pytest -n auto -q loremaster/tests/test_principals_cli.py` → **PYTEST_EXIT=0**,
  tail: `45 passed in 5.01s` (updated prog pin + new resolution pin + all frozen pins).
- `uv run ruff check loremaster/loremaster/principals.py loremaster/tests/test_principals_cli.py loremaster/pyproject.toml`
  → **RUFF_EXIT=0**, `All checks passed!`.
- `bash scripts/typecheck.sh` → loremaster leg `Found 102 errors in 8 files` — EXACTLY the
  #333 packet-39 (auth/posture) bound named in the brief; **ZERO errors in my touched files**
  (all 8 are pre-existing `test_auth_composition.py`-family files re PostureConfigError /
  resolve_posture / Posture / SCOPE_READ). The exit-1 is the RED_ADJUDICATED bound doing its
  job, not RED_ORPHANED. My change adds no new typecheck error.
- `uv.lock` delta: **NONE** — md5 `7cb7766991116e18c57c26e75e5b6a13` identical before and
  after `uv sync --all-packages`; `git status --porcelain uv.lock` empty.

## registration_sites
`./scripts/registration_sites.py` → NO new registration site attributable to this change.
- `lore-adm` / `[project.scripts]` appear NOWHERE in the output (grep confirmed).
- Structural reason: the entry `lore-adm = "loremaster.principals:main"` names exactly ONE
  member (`loremaster`); a co-occurrence site requires ≥2 distinct member names, so a
  one-member line cannot be a site. A `[project.scripts]` entry is not a workspace member —
  confirmed, not assumed.
- The two `loremaster/pyproject.toml` hits it reports (`:8` = the `dependencies` sibling list;
  `:62` = the `[tool.uv.sources]` sibling block) are PRE-EXISTING sibling-declaration blocks,
  unchanged in content and merely renumbered by my 6-line insertion (git diff shows my only
  pyproject change is the `[project.scripts]` block). The script's overall totals
  (52 sites / 22 incomplete) are the pre-existing baseline; my change adds none.

## Coordination
- Ledger row `2630ec43e7ec47c5b772c0cacdc232ea`: claimed → in_progress (owner
  `builder-49-adm`). Left in_progress for the lead to close on commit (I do no git).
- Idle-gate contract written: `/tmp/claude-idle-gate-lore/fdca8da8-...-builder-49-adm.contract`
  = `{"artifact": "REPORT-builder-49-adm.md"}`.
- NO git performed (lead commits). NO deploy. Writable set respected: only the 3 code files;
  the 3 dirty docs are not mine.
