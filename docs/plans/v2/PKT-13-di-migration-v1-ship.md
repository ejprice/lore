# PKT-13 — Execute the DI migration + v1.0 SHIP
size ~0.25 wu · wave M, LAST (re-waved 2026-07-14) · depends: PKT-11, PKT-12, PKT-24, operator GO/NO-GO
**v1.0 DEFINITION (operator-ruled 2026-07-14): the SINGLE-NODE local ship** — DI migrated,
singular store, onboarding fixed, single-node drills (PKT-10a) green. Client/server roles,
split topology, and hosted security are wave S, post-features, explicitly NOT v1.0 gates.
law: DESIGN-LAW §7 (I1–I4 — receipts before trust), §6 · DEPLOY: v1.0 tag + cutover receipts

## Mission
Run Shape D live on lore-demand_intelligence (per Q4's answer — default: dry-run
Phase A + GO/NO-GO first), then ship v1.0. DI is PARKED awaiting lore (operator,
2026-07-06) — this packet un-parks it.

## Scope IN
- **Phase A** online pre-warm (steps 1–4) → **operator GO/NO-GO** → **Phase B** freeze
  (5–7) → **Phase C** swap + verify (8–9): count parity + spot recalls BEFORE anything
  retires. **Retire (step 10) stays deferred to soak** — operator confirms later.
- **§8.5 token-accounting receipts** at ship: real schema+response costs vs the
  standing bar (the measured truth is 8,245 claude-equiv full-surface; #49's ACCEPT
  ruling — INDEX pool item 5 — lands here if not earlier).
- **§8.4 dogfood receipts** on both projects: drive the ladder on real questions;
  wait_for_fresh after a live edit.
- **v1.0 tag** + cutover receipts + a one-page implemented/partial/deferred
  architecture status note (external-review priority 7; the packet INDEX carries the
  forward half — this note carries the "what shipped" half).
- Schedule (not execute) the post-soak items: DI v0.3 retire, shared-Qdrant orphan
  cleanup, `QDRANT__SERVICE__API_KEY` removal — operator-owned, INDEX pool item 3.

## Entry check
PKT-12's dry-run receipts green; DI's lore.yaml upgraded by N6 (its missing surreal
block is the known lockstep prerequisite); both containers healthy.

## Exit
DI serving on v2 with parity receipts + spot recalls; v1.0 tagged; INDEX row + Log.
**v1.0 SHIPS HERE. Waves F/G are post-1.0.**
