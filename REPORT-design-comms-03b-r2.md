brief-base v6 read

# REPORT — design-comms-03b-r2 (re-certification protocol for packet 03b's contract phase)

## SUMMARY BLOCK

- brief-base v6 read
- **State: done.** Ruling doc written and standing by for follow-ups (long-running sidecar).
- **Deliverable:** `docs/plans/v2/receipts/2026-07-24-packet03b/DESIGN-03b-recertification.md`
  — RC0–RC10, section-numbered for citation. Measured 2026-07-24 at `6bfe820`.
- **Headline ruling (RC1): the lead's three-leg proposal is ADOPTED with corrections** — six
  named, each argued: the blind-build canon must include a lead-prepared vocabulary extract or
  contract-blindness manufactures false over-pin findings (RC2.1); adjudication is FOUR-way,
  not two-way — pin defect / ruling ambiguity / reference defect / ruling defect, all four
  measured realities in this packet's own record (RC2.3); P1 depth goes to the UNGRADED delta
  with the 43 prior survivors as a mechanical regression leg (RC2.4); and the proposal's
  biggest miss is the **merged-build all-8-suite gate** — no reference build has EVER
  co-satisfied both waves; every satisfiability receipt in the packet's history was a per-wave
  silo (RC2.5).
- **Taint sized precisely (RC0):** the ungraded corpus is exactly FOUR commits (`7bc6a08`,
  `00a92dd`, `86abf1e`, `6bfe820`) — everything before `45cc161` was independently graded by
  both adversaries, including satisfiability via their own reference builds. Authority:
  pre-adversary corpus RETAINED; post-adversary delta SUSPENDED-pending-grade; receipts STRUCK
  as certification, retained as history/recipes (marking text in RC6); design rulings RETAINED
  (RC5), with register/heartbeat annotation RULED required-in-scope (RC5.2).
- **Q5 (RC7): no rewrite ordered**; a NAMED numeric re-authorship trigger replaces the vibe.
- **Q3 (RC4): client instrument fully shaped** — 3 clients (one Sonnet/Opus/Fable, the
  operator's named population), a 12-scenario keyed battery derived from the ruled teaching
  obligations, keys authored before clients run, F1-class failures GATE, battery committed as
  a reusable eval for packets 04–06.
- **Decisions-needed (operator forks, RC10):** F1 spend · F2 batched weakening authorizations
  for confirmed overfit pins · F3 re-authorship if triggered · F4 leg-C scope findings ·
  F5 receipts-marking wording.
- Receipt pointers: RC0 (bucket table + commit bucketing probe) · RC2.1 (canon + withheld
  set + the `_annotate_trace`-is-in-S8-E2 probe) · RC2.3 (taxonomy + adjudicator) · RC2.5
  (merged gate + 20-consecutive + 12-skip audit) · RC3 (traceability + receipt sampling +
  authorization-condition discharge) · RC4 (battery table) · RC8 (ten misses) · RC9 (roster,
  never-reuse names).

## Body notes (read on demand)

**What I attacked in the lead's proposal before ruling** (the brief's requirement): the
"rulings ALONE" input set (under-specified — ruled vocabulary lives partly in the registry by
reference; fixed via the extract); the two-outcome adjudication path (the telemetry
adversary's own reference build violated three rulings — category-(iii) is real; D3 proves
category-(iv)); whole-battery re-runs (duplicate independently-bought coverage; delta focus +
survivor regression instead); per-wave-silo satisfiability (the merged gate is new work, and
it doubles as the first proof 03b's global-mypy-zero exit is reachable); blindness enforcement
(freeze-hash receipt + brief-withheld files + tool-honesty, with the six existing scratch
trees quarantined — the author trees ARE the frame under test); and leg C's vagueness
(operationalized as act-don't-summarize scenarios graded against pre-authored keys, 2-of-2
confirmation, per-class gate semantics).

**One fairness note for the record (everything-noticed law):** the process as run was more
guarded than the one-line characterization — two independent INSUFFICIENT adversary grades
happened and were answered, and the author reference builds caught real committed defects
(D1/D3/D7/D9) before any adversary ran. AND the operator's charge is correct at the layer
that matters: the final corpus has no independent grade, and the C-DEF law's designated
certification instrument (the adversary's build) was substituted by author builds at every
certification point. Both statements are true; the protocol targets the second without
burning the value of the first.

**Probes run (read-only, per brief):** `git log --oneline 0223291..6bfe820` (commit
bucketing, RC0); `grep -rn "_annotate_trace" docs/` (the symbol IS ruled — S8-E2 names it —
killing an expected false-positive class, RC2.1); `ls` receipts dir + repo root (marking
mechanics, RC6). No suites run; no lore needed (no code-structure question); no files edited
outside my two writable paths.

**Standing by** for SendMessage follow-ups with full context loaded (packet, both rulings
docs, all seven reports, the adversary role spec, INDEX state).

---
---

# POST-SUPERSESSION ADDENDUM (2026-07-24, after the operator's second directive + lead reset to `0941aab`)

The operator superseded the RC-protocol at the corpus-disposition layer; the lead reset the
test tree to the 03a-2 close and launched a blind fresh derivation. My doc carries the
supersession banner (RC0/RC5/RC7 retention rulings VOID; RC2.3/RC2.5/blindness-mechanics/RC4
survive as adopted instrument designs). I am re-tasked as DIFF-ADJUDICATOR for when
`03b-design-rulings-r2.md` lands, under the binding rule: every amendment candidate carries a
derivation from trusted sources or a fresh probe — the struck material may only point at a
question, never answer one.

## The empirical-hazard checklist (task 3) — discoveries in the struck record that took
## EMPIRICAL work, which a blind derivation from trusted sources at `0223291` cannot re-derive
## by reasoning. One line + receipt each. Receipts cite struck material AS POINTERS ONLY
## (each names a question to re-probe/re-derive, never an answer to inherit). All struck
## reports at tag `pkt03b-tainted-corpus` / the struck-banner archive.

**A — Baseline contamination: the reset RESTORED five measured-defective committed pins**
(their fixes were operator-authorized amendments 1–7, struck with the session; at `0941aab`
the defects are LIVE and unmarked in the tree):
- A1 The fleet-elision marker rewards the wrong drain build — measured both legs: ruled-correct
  arithmetic → 2 FAILED, fleet's `shown+more` → 2 PASSED (REPORT-contract-surface-03b §D1).
- A2 ACK-REQUIRED proofs are an `acked_at` monoculture — a grade-only build passes the
  committed proofs 25P/0F (same report, mutation row 1; packet amendment 1/R3).
- A3 The value-carried bound's pin asserts `"{msg}"`-only while its docstring bans the class —
  P2 false-gate, live (amendment 2/R5).
- A4 The reach-pin `expected` set omits the three new render helpers (amendment 3/R6); no
  `send.thread` injection case exists (amendment 4/R7).
- A5 The 2 `test_message_ledger.py` mypy errors are back, and building the surface will NOT
  pay them (`Returning Any…` in `_ask`/`_answer`; packet amendment +6).

**B — Engine facts, probed live on 3.2.1** (cannot be reasoned out; each is a one-minute
re-probe on `:18000`):
- B1 `CREATE … CONTENT $x` composes with neither `SET` nor `MERGE` (parse errors);
  `object::extend($x, {seq: sequence::nextval(…)})` is the only bind-plus-computed-column
  shape (REPORT-contract-telemetry-03b §5.1) — cost the discoverer real pin deaths.
- B2 `SELECT *` OMITS a NONE-valued column entirely (`KeyError`) — NOT the same as the
  documented missing-projection-reads-None (§7.1 there).
- B3 `sequence::nextval` starts at 0 under default START (§5.1).
- B4 Prod `:18500` trace `count() = 0` is SINGLE-SOURCED (scout probe; the adversary's
  re-derivation was refused by the environment classifier — adversary-telemetry §4/R9).
- B5 Status question: the two B1/B2 facts were landed into the store reference at `7f23223`
  by the struck session — verify whether that edit survives the docs strike; if not, re-probe
  and re-land (an engine parse error is true regardless of who probed it).

**C — SDK facts, measured on `mcp` 1.27.2** (must be re-MEASURED, not reasoned):
- C1 One `ServerSession` object per MCP session over real streamable-http (rung-1 fact;
  independently corroborated by `is`-comparison with objects held alive —
  contract-telemetry §3; adversary-telemetry §4.1).
- C2 `mcp` 1.27.2 has NO middleware API; `FastMCP.call_tool` is a two-line delegate
  (scout §5; forces the subclass-override shape).
- C3 Cancellation: one `task.cancel()` delivers ONE `CancelledError`; a plain `finally`
  write completes (no `asyncio.shield` needed) — while `except Exception`-keyed emission
  writes 0 rows on a cancelled dispatch vs 1 (WB30; telemetry-fix MP2 section).

**D — Caller-identity empirics (the WB9 cluster — design reasoning produced the defeated
"no collision path" row; only adversary work found the truth):**
- D1 `caller = str(id(session))` passes every stability + distinct-while-COEXISTING pin and
  collapses 400 sequential sessions → 17 keys (383 collisions); the needed quantifier is
  distinct-∀-TIME (adversary-telemetry §1.3). Magnitude of `id()` reuse: 198/200 (§4.2).
- D2 The MP3 INTERLOCK: a retaining plain dict passes uniqueness (no GC ⇒ no recycling) but
  fails retention; weak retention forces recycling so an identity key fails uniqueness —
  NEITHER pin alone corners the build (telemetry-fix MP3; WB32: 500 retained entries vs 0).

**E — Wrong builds that survived FULL batteries** (each a checklist row for grading the
fresh contract, not a design prescription):
- E1 The no-op fix, twice: `_render_comms_send`/`_render_comms_ack` built perfectly, never
  called; hardcoded `"no unread messages"` passed 1092/1092 (adversary-surface B1/B2).
- E2 Instructions-paragraph substring gaps: serving a 20000-char body cap, or INVERTED
  peek/grade semantics, passed 1092/1092 (W16–W19); `"ack"` matched vacuously inside
  `"brief_ack"` (m3).
- E3 The ACK-REQUIRED taught command's tail: `…action=ack seqs=[]` survived — the proof
  marker stopped one character before the part that must be true (W6).
- E4 The fix-wave second-axis pattern: closing `acked_at` left the GRADE axis open — an
  unacked SIGNAL listed in the demand survived (W41); measured precedent that fix waves
  open/leave axes.
- E5 Cross-session broadcast leak when `session=` omitted (legal) — wave7 traffic delivered
  into wave9; invisible because 13/13 drivers passed `session=` (W32).
- E6 The S1 coverage-premise probe RESULT (not the method): new prefixed helpers in
  `server.py` are seen default-FAIL (49 literals / 9 helpers floor); an UNPREFIXED helper is
  INVISIBLE (#145 negative control) — must be re-probed at the clean phase's tree, never
  assumed (struck S1 table is the pointer).

**F — Fixture-arithmetic traps, measured:**
- F1 `served ≡ limit` alignment: one drain fixture can never separate hit_count=served from
  =limit (served is ALWAYS pending or limit); at default limit 20 with 1 seeded the wrong
  build records 20 — the exact number packet 06's decay curve reads (adversary-telemetry
  §3.1; WB2).
- F2 Boolean/value monocultures that blinded whole branches: `broadcast=False` at 100% of
  call sites; `_p03_entry` HARDCODED `thread` with no parameter at all (a monoculture with
  no dial — D10 wave).

**G — The graders' own instruments failing (self-trap class):**
- G1 D3: the design-prescribed `_has_literal_text` ban was a false gate in BOTH directions —
  RED on the correct build's `" "` separator AND `_has_literal_text("{msg}") is True`
  (executed, conceded — struck rulings S8-D3 as pointer).
- G2 MP7 first draft: index-fields check passed via the index NAME `trace_caller_seq`
  containing both substrings — split on `" FIELDS "` was required (telemetry-fix mutations).
- G3 Four vacuous passes: `all()`/`sorted()`/`len(set())` trivially green on empty lists;
  "call succeeded while store broken" trivially green with no telemetry at all
  (contract-telemetry §4.1).
- G4 D7: the deny-by-default canonicaliser DENIES `GeneratorExp`; `render_join(",", …)`
  introduces an unclassified separator; the working shape was measured, not guessed
  (surface report §D7).
- G5 D9: the `SimpleNamespace` harness double hid TWO missing production dependencies at
  1060 passed / 0 failed (`CommsConfig.drain_limit`, `AppContext.message_ledger`) — only a
  full reference build's typecheck saw it (surface report §D9).
- G6 The seq-type incentive: at `0941aab` the committed schema/store/fakes suites STILL
  structurally reward the wrong schema choice — correct `seq int` → 7 committed pins RED;
  `option<int>` → 2 RED; a five-test incentive with no gate stopping the flip
  (adversary-telemetry §6.1). Any fresh design requiring a new mandatory column meets this.

**H — Cross-wave couplings, measured:**
- H1 The telemetry contract cannot go 0-failed on a telemetry-only build — 39/41 without the
  sibling's `drain`+`peek` (adversary-telemetry §0/R10).
- H2 Global mypy-zero is unpayable by either wave alone — 6 `record_trace(caller=…)` errors
  survive a full surface build (surface-fix §5); the "36→0 structural" claim was
  measured-false twice.
- H3 The harness derived-count pin fires CROSS-suite: one new harness-importing test file
  broke it (35→36 importers AND the masked 21→22 `connect_admin` count behind the first
  assert; surface-fix Part B) — recurs on the clean phase's first new test file.
- H4 No reference build has ever co-satisfied all suites at once (my RC2.5 finding, adopted).

**I — Production prose corpses, live at `0941aab`** (production untouched by 03b, so these
are in the tree the blind designer reads — and no gate checks prose):
- I1 `record_trace`'s docstring teaches "fire-and-forget" — the posture the struck design
  REFUSED, but the refusal is struck with it; a blind designer may inherit the docstring's
  posture as intent (adversary-telemetry §6.2 enumerates all six corpses: "six core fields",
  `TraceSummary` "no caller yet wires", the schema header comments, etc.).

**Input-set FLAGS (not hazards — forks, surfaced per scope law):**
- FLAG-1 Operator rulings recorded ONLY in struck-session commits post-`0223291`: the
  all-tools scope widening (`75e3742`), the R1→packet-05 deferral + amendments 1–6
  authorization (`cda838d`), the standing authorization (`9a98cf3`), the WB9/E3 rulings
  (`14b260f`). A `0223291`-only canon re-derives DRAIN-ONLY telemetry — which the operator
  personally overruled. Which operator rulings ride into the blind canon is the lead's/
  operator's call, not mine.
- FLAG-2 Whether the operator-authorized amendment content (cluster A's fixes) is struck
  WITH the session or survives AS operator authority is the same fork from the other side.
- FLAG-3 `7f23223`'s store-reference landing (B5 above) — trusted-doc status needs a ruling.

**[FLAGS RULED by the lead, 2026-07-24, after commit `a2e3847` — recorded here so the flags
above read as closed, not dangling; the rulings of record live in the packet file:]**
FLAG-1: covered by construction — the blind designer's brief transcribed the post-`0223291`
operator rulings verbatim (all-tools widening incl. rationale, R1→05 deferral, client
directive, contract immutability); WB9/E3-class struck CONCLUSIONS deliberately excluded —
this checklist's clusters D/G are the designed recovery at the diff step, with fresh probes.
FLAG-2: conservative reading — struck measurements (operator-authorized amendment substance
included) are LEADS requiring independent re-derivation; committed-pin fixes land only under
FRESH operator authorization. Cluster A is now **finding #182** and a named BLOCKER list in
the packet file: no builder briefed while any of the five is unadjudicated.
FLAG-3: re-probe-on-contact, owned by the fresh adversary, recorded in the packet file.
This checklist is the diff step's completeness grader, cited from the packet file.
