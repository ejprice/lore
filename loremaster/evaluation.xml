<?xml version="1.0" encoding="UTF-8"?>
<!--
  Phase-4 evaluation suite for the lore (loremaster) MCP server.

  Each qa_pair is INDEPENDENT, READ-ONLY, COMPLEX (multi-tool exploration of the
  indexed lore monorepo), REALISTIC, VERIFIABLE (single string-comparable answer),
  and STABLE (a fact about committed code/design, not a drifting count/timestamp).

  Questions are phrased as tasks so the harness/model picks the tools; they do NOT
  depend on exact tool names (the tool set may gain a `lore_` prefix in v0.2.0).

  All answers were verified against the live server at http://127.0.0.1:9202/mcp.
  See EVALUATION.md for how to run.
-->
<evaluation>
   <qa_pair>
      <question>This project's server performs a startup gate that probes the embedding endpoint and verifies vector-dimension coherence before it will begin serving. When that gate decides the server must NOT come up — because the endpoint is unreachable or a dimension disagrees — it raises a single dedicated exception type defined in the server module. What is the exact name of that exception class?</question>
      <answer>ProbeGateError</answer>
   </qa_pair>
   <qa_pair>
      <question>The server's heavy startup work (the probe gate, the initial reconcile, and starting the file watcher) is wrapped so that it runs exactly once per process even though FastMCP's streamable-HTTP composition would otherwise re-enter the lifespan once per client session. A reference-counted "lease" mechanism on a single class enforces this once-per-process behavior. What is the exact (qualified-name-leaf) name of that class?</question>
      <answer>_ProcessLifespanGuard</answer>
   </qa_pair>
   <qa_pair>
      <question>In the embedding-resilience layer, when the backend rejects an input as too long, the wrapper bisects that input by tokens, embeds the pieces, and combines them back into a single vector. Identify the helper function that performs that final combine-into-one-vector step, then determine: under how many distinct conditions does that helper return None instead of a vector? Answer with just the number.</question>
      <answer>3</answer>
   </qa_pair>
   <qa_pair>
      <question>The indexer turns source files into chunks before embedding them. The chunk data structure it consumes is defined in one of the sibling chunker packages, not inside the server package. From which Python module does the indexer import that chunk model class? Give the fully-qualified dotted module path.</question>
      <answer>lorescribe.models</answer>
   </qa_pair>
   <qa_pair>
      <question>Each stored vector point gets a deterministic id derived from a versioned natural key. The keying scheme was deliberately re-keyed once (a breaking change) when an ownership dimension was folded into the key, and the scheme carries an integer version constant so the change stays detectable. What is the current integer value of that key-version constant? Answer with just the number.</question>
      <answer>2</answer>
   </qa_pair>
   <qa_pair>
      <question>The self-hosted embedding backend rejects a request with HTTP 413 if a single request carries more than a fixed number of input texts, so the client caps each request below that limit. What is that per-request maximum text count the client enforces? Answer with just the number.</question>
      <answer>32</answer>
   </qa_pair>
   <qa_pair>
      <question>When a transient embedding failure (such as a rate-limit or a server error) triggers the retry path, the wrapper sleeps for an exponentially growing delay, but the delay is clamped so a long outage never produces an absurd sleep. In seconds, what is that maximum (cap) on a single backoff delay? Answer with just the number (it may include a decimal point).</question>
      <answer>30.0</answer>
   </qa_pair>
   <qa_pair>
      <question>The project ships a durable, project-scoped memory store that lives in its own dedicated vector collection, separate from the code collection. Given a project whose slug is configured as "myrepo", what would the exact name of that memory collection be? Provide the literal collection name.</question>
      <answer>lore_myrepo_memory</answer>
   </qa_pair>
   <qa_pair>
      <question>The embedding abstraction package is intentionally decoupled from the server: the server reaches the backend-selecting factory through ONE thin translation seam, so swapping backends is a config edit rather than a code change. Across the entire indexed repository, exactly one NON-TEST module imports that backend-selecting factory module directly. What is the fully-qualified dotted name of that single importing module?</question>
      <answer>loremaster.embedding</answer>
   </qa_pair>
   <qa_pair>
      <question>Use the code-to-test graph to find the tests covering the server's startup dimension/reachability gate function (the routine that probes the embedder and refuses to start on a mismatch). All of the related test nodes live in a single test file. What is the base filename of that test file (just the filename, e.g. test_something.py)?</question>
      <answer>test_mcp_server.py</answer>
   </qa_pair>
   <qa_pair>
      <question>The token counter used to size inputs is exact and fully offline: it never downloads a tokenizer from the network, instead loading a pinned tokenizer file that is committed alongside the embedding-abstraction package on disk. What is the base filename of that committed tokenizer file (just the filename, including its extension)?</question>
      <answer>voyage4_tokenizer.json</answer>
   </qa_pair>
</evaluation>
