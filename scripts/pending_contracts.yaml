# pending_contracts.yaml — the ACCEPTED, PENDING-BUILD bounds of this repo's
# canonical gates. Read by scripts/pending_contract_gate.py, which DENIES BY
# DEFAULT: every gate line must be accounted for here, or the gate fails loud.
#
# ─────────────────────────────────────────────────────────────────────────────
# WHAT THIS FILE IS, AND WHAT IT IS NOT
# ─────────────────────────────────────────────────────────────────────────────
# It is NOT a suppression list. Nothing here silences anything: the plain gates
# still run unchanged, every error they emit is still printed, and this file only
# decides which of them have already been ADJUDICATED. An entry buys a specific
# error in a specific file naming a specific symbol — nothing wider.
#
# It is the PIN on an accepted bound, per this repo's standing law: "an unpinned
# known limitation is indistinguishable from an unknown one", and "per standing
# law, an accepted bound is PINNED, never prose". Finding #306 is the diagnosis;
# this file is the instrument.
#
# ⚠ IT SELF-DESTRUCTS. A registered file that stops erroring, a registered symbol
# that stops appearing, a symbol that becomes importable, or a code allowance
# whose count moves — each one FAILS the gate until the entry is deleted with the
# fix. The bound cannot outlive its premise. If you are reading this because the
# gate told you to delete something: that is the instrument working. Delete it.
#
# ─────────────────────────────────────────────────────────────────────────────
# ⚠ UPDATE 2026-08-22 — packet 39 WAVE 1 discharged 10 of the original 11 files
# ─────────────────────────────────────────────────────────────────────────────
# The historical measurement below (533d917 / 2026-08-01: 11 files / 198 errors)
# is HISTORICAL — do NOT read it as current. Packet 39's build STARTED; wave 1
# (identity & admission; cold-audit-39-w1 VERDICT GO, 2026-08-22) discharged 10 of
# the 11 registered files: 8 were archived to
# docs/plans/v2/receipts/2026-08-21-packet39-recut/, test_email_normalisation.py
# greened (lorerunes.normalize_email now exists), and test_auth.py's stale
# LoreTokenVerifier/GoogleOAuthConfig pins were retired (ApiKeyVerifier keepers
# stay green). R1 (routed by the cold audit) pruned the 26 self-destruct orphans
# this wave discharged. The `bounds:` section now registers ONLY the 1 remaining
# WAVE-3 forward-reference. Re-derive (`--derive`) before citing any count.
#
# ─────────────────────────────────────────────────────────────────────────────
# DERIVATION — what below is measured, and what is a ruling
# ─────────────────────────────────────────────────────────────────────────────
# DERIVED, by `uv run python scripts/pending_contract_gate.py --derive` against a
# live `./scripts/typecheck.sh` run: the file set, per-file `missing_symbols`,
# per-file `unsymboled_codes` counts. HAND-AUTHORED, because no script can derive
# an adjudication: `owner`, `reopen_trigger`, `ruling`, `rationale`, each
# allowance's `reason`. Re-derive rather than hand-editing; a hand-list is the
# artifact this repo has the most receipts against.
#
# HISTORICAL MEASUREMENT (533d917, 2026-08-01, superseded by the wave-1 update
# above): `./scripts/typecheck.sh` reported 198 errors over 12 files (191 in the 11
# registered files + 7 in packet 04b-2's then-in-flight C3 contract). Codes: 190
# attr-defined, 4 call-arg, 4 no-any-unimported.

version: 1

bounds:
  - id: packet-39-pending-build
    owner: >-
      packet 39 WAVE 3 (composition) — the sole remaining closure owner after
      wave 1 (2026-08-22) discharged the identity/admission layer. (Original PIN
      owner: packet 04b-2 wave C; CLOSURE owner: packet 39.)
    reopen_trigger: >-
      packet 39 WAVE 3's build, which wires build_mcp_server's injected http_client
      seam (design docs/design/2026-08-21-packet39-recut-oauth.md §4.3/§8). The
      single remaining entry self-destructs when _auth_fixtures.py's HOSTED branch
      stops referencing the not-yet-wired build_mcp_server(http_client=) parameter.
    ruling: >-
      finding #306 (diagnosis + ruling text), packet 04b-2 wave C design sidecar
      ruling 4. The wave-1 discharge (10 of 11 files) is confirmed by
      cold-audit-39-w1 (2026-08-22, VERDICT GO), which routed R1 to prune the 26
      self-destruct orphans this wave discharged.
    rationale: >-
      The one remaining error is a WAVE-3 forward-reference, deliberately NOT
      type:ignore'd: _auth_fixtures.py's wire_session HOSTED branch calls
      build_mcp_server(http_client=), the injected-client seam packet 39 wave 3
      wires (§4.3/§8). It is DEAD AT RUNTIME today (the live consumer
      test_refusal_observes_effect uses posture=loopback, so the HOSTED branch
      never executes). Masking it would hide the wave-3 seam signal. This is the
      out-of-authority deferral shape with a named decision point (wave-3 build),
      not unfixed work.

    files:
      - path: loremaster/tests/_auth_fixtures.py
        missing_symbols:
          - build_mcp_server.http_client
